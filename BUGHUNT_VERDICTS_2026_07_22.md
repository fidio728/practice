# BUG HUNT 最终判定档案 (geoecon julia_descriptive)

工作流 wf_a8145a78-f88。43 原始发现→去重 24 条(B1-B24)。反驳机制:每条 2 个独立反驳者,双票 refuted 才 REFUTED。

**进度: B1-B13 已查(有票), B14-B24 未查(0票,额度中断). 已查部分模型=Fable5,建议关键条 Opus 复核.**

排除(已知,不在24条内): C1-C6 审计项、已标 flagged-for-advisor 的选择、图脚本。

# ===== CONFIRMED (存活, 双票或单票未被证伪) =====
## [B2] MAJOR — F2 phantom-row (in_span) 修复未传播到任何衍生估计面板:ladder、S_agg、shocklag、ownership、tail 及 Russia 正控制
**file**: julia_descriptive/build_russia_c6_panel.py:51  dims=design,grid
**claim**: Cartesian grid 对每个曾出现的公司铺满 1999Q1-2023Q4,~25.03% 的 regressor-complete 行是 pre-IPO/post-delisting phantom 零(build_audit_panel_f1f2f7.py:10 已量化)。in_span 补救列只存在于 audit_c6_panel.dta;其余所有估计面板 — c6_panel.dta(run_tail_3pairwise.do / run_ddd_nofe_bil.do 使用)、sagg_panel.dta、shocklag_panel.dta、ownership_c6_panel.dta、c6_panel_russia.dta(run_russia_headline.do 使用),以及 advisor 确认的 Step-1/Step-2 ladder 面板(build_firm_ladder_panel.py:49-62、build_country_panel.py:47-74 仅按 w/cn_lag 非空过滤)— 均无 in_span 标志,在污染样本上估计且无法在 Stata 端事后过滤,相关 do 文件也不披露该 caveat。
**failure_scenario**: China 侧:pre-IPO firm-quarter 以两组 dw=0 进入而 cn_lag>0、shock 变动,机械把 β₃ 拉向 0,恰在无法做 in-span 检查的 advisor-facing 修订(S_agg、S_{t-1}、flow)中美化 null;ladder 的 LEVEL 列中 phantom 零不被组内差分消掉,唯一显著 cell(firm M3 +8.6e-5)estimated on 污染样本、in-span-only 是否存活未测。Russia 侧:2022Q1 后退出 FactSet 覆盖的暴露公司在退出季贡献 -(w_US,last - w_NONUS,last) 加后续 dw=0 ghost,Russia β₃<<0 部分反映机械覆盖退出,且正控制引自未过滤网格而 China headline 引自 in-span — 控制与 null 并非平行样本。
**反驳者1** refuted=False | severity_adjust=Downgrade major → minor. The non-propagation is real but its consequences are the opposite of alleged where it matters most: the Russia positive control strengthens (not weakens) under in-span filtering, the parallel-samples charge is factually wrong, and the China attenuation is quantified, documented, and conclusion-preserving (null either way, p 0.44→0.43). Residual actionable items: (1) re-run the significant firm-ladder M3 LEVEL cell with an in-span filter, since phantom zeros in LEVELS are not differenced away; (2) add a one-line caveat + in_span column to derived panels or their do-files for disclosure hygiene.
  reasoning: Code-side assertions all verified true: in_span exists only in audit_c6_panel (grep over julia_descriptive), build_russia_c6_panel.py:51 cited accurately, ladder/sagg/shocklag/ownership/tail/Russia panels and their do-files have no span flag or caveat, and I reproduced the 25.0344% phantom share exactly under the c6_panel filter. So the core claim (F2 fix not propagated) cannot be refuted. However, the failure_scenario is substantially wrong on the Russia side, which I tested directly: using the fq+gq-equivalent within-firm-quarter diff regression (validated by matching the documented China numbers, full 1.28e-6 / in-span 2.38e-6), the Russia positive control goes from -7.41e-6 (full grid) to -1.27e-5 (in-span) — dropping phantoms makes the control 71% STRONGER, so the bug biases AGAINST the positive control rather than manufacturing it; the coverage-exit ghost rows (43 firms, 53 nonzero-dw rows of 462k) are swamped by dw=0 dilution. The "control and null are not parallel samples" charge is also false: both cited numbers are full-grid (in-span is a separate F2 robustness row). On the China side the attenuation direction is right but immaterial: in-span moves the null from p=0.44 to p=0.43, was already quantified and documented (run_audit_f1f2f7.do, run_headline_3pairwise.do, methodology tables lines 59-61, 549). What genuinely survives: ladder LEVEL specs (the one significant firm-M3 cell) never re-run in-span, and do-files lack disclosure.
**反驳者2** refuted=False | severity_adjust=major -> minor (code claim accurate but all predicted estimation consequences refuted or already measured-immaterial; remaining issue is missing caveat disclosure and missing in-span robustness rows in derived do-files)
  reasoning: Code facts verified as accused: in_span exists only in audit_c6_panel.dta; ladder/sagg/shocklag/ownership/tail/Russia panels are full-grid with 25.03% out-of-span rows (reproduced). But every predicted harmful consequence fails an independent replication test (pair-differenced estimator matching published reghdfe numbers to 3 digits): (1) Russia positive control gets STRONGER in-span (b3 −7.3e-6→−1.28e-5; event −9.0e-5→−2.5e-4), so mechanical coverage-exit inflation is refuted — full grid is conservative; (2) the "non-parallel samples" charge is factually wrong — the published China headline (+1.80e-6 p=0.35) is also full-grid, in-span is only the F2 robustness row; (3) the ladder M3 significant cell survives in-span (+8.6e-5 t=2.01 → +1.44e-4 t=1.99); (4) China-side attenuation is modest (~25-35%), uniform toward zero, already quantified and published via F2 (p=0.43, RI 0.58) — no null verdict flips; (5) post-hoc Stata filtering IS possible via a trivial span merge on firm_str/rdate. Residue: derived do-files omit the caveat and in-span variants — a disclosure/robustness-coverage gap, not a result-changing bug.

## [B3] MAJOR — in_span 标志不截尾超过 last_active 的 lead/累计结果;docstring 声明相反
**file**: julia_descriptive/build_audit_panel_f1f2f7.py:18  dims=grid
**claim**: audit_c6_panel 的 F1 结果 dw_lead1 与 cum1..cum4 用 LEAD 建在完整 zero-filled 网格上,而 in_span 只检查行自身的 rdate(line 77)。位于或接近 last_active 的行 in_span==1,但其 cum_h = w_{t+h} - w_{t-1} 读到 last_active 之后的补零季度。docstring(lines 17-19)声称 'span-boundary leads are not corrupted',对退出边界为假:即便 F2-cleaned 的 LP/lead 规格也把 phantom 退出 -w 嵌入结果变量。
**failure_scenario**: 公司最后持有期 2020Q4:其 2020Q3 行 in_span=1 但 cum4 = w_2021Q3 - w_2020Q2 = 0 - w_2020Q2,是一次完全机械的撤资。若覆盖退出在高 shock 季度聚集于暴露公司(如 2022),被引为 headline F1b 确认的 LP IRF 在 h>=1 捕捉的是覆盖流失而非交易;'in-span' 稳健性重跑仍被污染,因为该标志无法截尾 lead 窗口。
**反驳者1** refuted=False | severity_adjust=维持 major。5.2% 的 cum4 样本污染 × 28 倍量级差,且退出聚集于高 shock 年份的暴露公司,机械 −w 分量与"交易性撤资"同号,足以污染被引为 headline 确认的 F1b LP IRF(h≥1);in_span 过滤按构造无法修复 lead 窗口,需在面板构建端加 rdate+h<=last_active 截尾。
  reasoning: 逐项核对均证实指控:(1) build_audit_panel_f1f2f7.py:77 的 in_span 仅约束行自身 rdate,无 lead 窗口截尾;lines 60-64 的 LEAD 建在 merged_us_eu_zero_filled.parquet 完整网格上。(2) 06_cartesian_grid.jl:253-257 证实退出后 w=COALESCE(I_ict,0)/total=0(非 NULL),跨界 lead 读到机械零。(3) run_headline_3pairwise.do:39-45 的 F1a/F1b 行确实无 in_span 过滤。(4) duckdb 复现面板:8,314/12,743 家公司在网格终点前退出;in_span==1 且 cum4 非空的 318,776 行中 16,666 行(5.2%)lead 窗口跨越 last_active,其中 10,039 行 w_prev>0;跨界行 cum4 均值 −5.7e-5 vs 非跨界 −2e-6(约 28 倍机械负值);cum1 亦有 1.5% 跨界。(5) 退出确在高 shock 年份聚集且含暴露公司(2023: 984 家退出、mean shock +3.0、93 家暴露;2021: +3.7;2018: +7.5)。docstring "span-boundary leads are not corrupted" 对退出边界为假(仅入口边界/相邻性成立)。failure_scenario 在数据中真实发生,指控无法证伪。
**反驳者2** refuted=False | severity_adjust=major -> minor
  reasoning: 代码事实层面指控成立:build_audit_panel_f1f2f7.py:60-64 的 LEAD 建在完整 zero-filled 网格上,line 77 的 in_span 只检查行自身 rdate,无 rdate+h<=last_active 截尾;docstring 16-18 行'span-boundary leads are not corrupted'对退出边界为假;run_headline_3pairwise.do:39-45 的 F1a/F1b 确无 in_span 过滤;06_cartesian_grid.jl:250-257 确认 last_active 之后 w 补零。failure_scenario 的前提也成立:8,314/11,961 家公司在 2023Q4 面板结束前退出;退出季度平均 shock 0.251 vs 无条件 0.063(退出确实聚集在高 shock 季度);in-span cum4 样本中 5.2%(16,666 行)的 lead 窗口越过 last_active,其中 60% 是机械退出(w_l4=0,w_prev>0),平均 cum4 幅度约为 interior 的 28 倍。但预言的后果被两点机制抵消:(1) last_active 是 firm 级,phantom 退出对同一 firm-quarter 的 US/EU 两行完全对称(boundary 单元格严格配对 7,334/7,334、999/999),headline 的 firm×quarter FE 吸收了共同分量,只剩 US−EU 微小差分(exposed 单元 −2.25e-4 vs −1.81e-4)可漏入 β3;(2) 该残余漏入为负,而估计的 β3 为正,方向是衰减而非制造效应。决定性检验(用 duckdb 重建含 ok1/ok4 截尾旗标的面板,Stata reghdfe 完全复现 .do 的 fq gq ig + 双向聚类):cum4 as-run b3=6.62e-6 p=.085 → 完全清洁(in_span+截尾)7.62e-6 p=.063;cum1 2.37e-6 p=.034 → 2.55e-6 p=.050;dw_lead1 四个变体全 null(p≈0.88-0.92)。所有系数同号、变化 <15%、无显著性反转,且清洁后略更强——as-run 估计反而偏保守。'LP IRF 在 h>=1 捕捉覆盖流失'的预言被证伪。缺陷本身真实(docstring 错误陈述 + 缺一行截尾旗标,建议补 ok_h 旗标并改 docstring),但对 F1 全部系数量化上无实质影响,故不 refute 指控的代码事实,severity 由 major 降为 minor。

## [B7] MAJOR — china_share includes COMPETITOR edges; doc defines it as five supply-chain rel_types only
**file**: julia_descriptive/02_china_exposure.jl:348-375, 409-430, 544-549  dims=doc-drift
**claim**: Essay2_methodology_full.md:183 (§4.2) 把 R_{i,t} 定义为五类供应链 rel_type(CUSTOMER, SUPPLIER, PARTNER-JVENTUR, PARTNER-MANUFAC, other PARTNER-*)之和,但代码在任何地方都不施 rel_type 过滤:eu_china_edge(02:348-375)与 eu_any_edge(:409-430)取全部边,n_cn_total 是全 rel_type 的 COUNT(*)(:549),china_share 分子分母都含 COMPETITOR 关系。
**failure_scenario**: 审稿人或复现者按 §4.2 认为 CN_{i,t-1} 度量对华供应链依赖;实际上仅有中国竞争者标签的欧洲公司也获得正 china_share 并可能被分入 HIGH(0.0476 中位数切点建立在混合度量上)。实际估计的 estimand('含中国竞争者的暴露')异于文档 estimand;按文档公式忠实重建会得到不同回归元、不同 HIGH/LOW 构成与不同 β₃,论文的供应链撤离经济解释与被测对象不符。
**反驳者1** refuted=False | severity_adjust=维持 major。混入的 COMPETITOR 占 CN edge-quarter 计数 14.85%,986 家公司仅凭竞争者关系获得正 china_share,4,984 个 firm-quarter 行仅凭竞争者关系越过 HIGH 切点,足以改变 HIGH/LOW 构成与 β₃ 的解释;这不是边缘性措辞问题。
  reasoning: 证伪失败,指控在代码、文档、数据三个层面全部核实。(1) 代码层面:02_china_exposure.jl 当前版本中,eu_china_edge(348-375 行)与 eu_any_edge(409-430 行)均从 rev_rel 取边且 WHERE 子句只过滤 region,全文件没有任何 rel_type 过滤(上游 rev_rel_raw 261-271 行、rev_rel 293-337 行同样无过滤);549 行 n_cn_total 是全 rel_type 的 COUNT(*),596 行 china_share = n_cn_total / n_total_links,分子分母均含 COMPETITOR。(2) 文档层面:Essay2_methodology_full.md 第 183 行(§4.2)确实把 R_{i,t} 定义为五类供应链 rel_type(CUSTOMER、SUPPLIER、PARTNER-JVENTUR、PARTNER-MANUFAC、other PARTNER-*)之和,与代码不符。(3) 数据层面:用 duckdb 查 output/firm_quarter_china_exposure.parquet 复现出与指控完全一致的数字——SUM(n_cn_total)=240,172,五类之和(n_cn_customer+n_cn_supplier+n_cn_partner_any)=204,500,差额 35,672(14.85%);18,147 个 firm-quarter 行 n_cn_total 超过五类之和;原始 data_giorgio.csv 中 COMPETITOR 边 935,751 条,且 rel_type 全集里 CUSTOMER/SUPPLIER/PARTNER-* 之外只有 COMPETITOR 一类,故差额全部来自 COMPETITOR。failure_scenario 也真实发生:8,286 个 firm-quarter 行的 CN 暴露完全来自非五类(即仅有中国竞争者),涉及 986 家不同公司,其中 4,984 行 china_share 超过 0.0476 的 HIGH 切点——即仅凭中国竞争者标签就被分入 HIGH 组的情形在落盘数据中大量存在。文档 §E next-steps 的 '#6 rel_type filter' 条目也旁证作者自知无过滤。无任何反证可用:代码存在且未被误读,场景在数据中真实出现,行为与文档定义不一致。severity=major 合理:实际 estimand(含竞争者的中国暴露)与文档声明的供应链依赖 estimand 不同,按文档忠实重建会得到不同的回归元与 HIGH/LOW 构成。修复路径可以是改文档措辞或加 rel_type 过滤做稳健性,但 doc-drift 本身成立。
**反驳者2** refuted=False | severity_adjust=keep major
  reasoning: Accusation fully confirmed by independent reproduction. (1) Code: no rel_type filter exists anywhere in 02_china_exposure.jl — eu_china_edge (348-375), eu_any_edge (409-430) take all edges; n_cn_total is COUNT(*) at line 549. (2) Doc: Essay2_methodology_full.md:183 defines R_{i,t} as five supply-chain rel_types only — definitional drift is real (though line 190 quotes the actual SQL, an internal inconsistency). (3) Data: parquet numbers reproduce exactly (SUM n_cn_total=240,172 vs five-sum=204,500, 14.85% excess, 18,147 rows); raw data_giorgio.csv contains only CUSTOMER/SUPPLIER/PARTNER-*/COMPETITOR, so the excess is entirely COMPETITOR. (4) Predicted consequence materializes: 8,286/75,860 (10.9%) positive-exposure firm-quarters have china_share>0 purely from COMPETITOR; 4,984/51,527 (9.7%) HIGH rows at the 0.0476 cut have ZERO supply-chain CN edges (robust to any denominator fix); 698/5,600 ever-HIGH firms (12.5%) are HIGH only via COMPETITOR; 13.5% of HIGH rows flip out under a clean numerator. Mitigation exists (rank corr 0.94 contaminated-vs-clean, identical positive-share medians, only 1.3% of all rows flip under recomputed own-median cuts, doc §E flags the missing filter as a next-step), so the measure is contaminated rather than destroyed — this justifies not upgrading to critical, but the estimand mismatch for ~10% of the treated group is material for a treatment-defining variable, so major stands.

## [B8] MAJOR — §7.6 'not a zero-fill artifact' check ran on the superseded dos outcome, not the FLOW primary outcome
**file**: julia_descriptive/test_obs_only.do:28, 32  dims=doc-drift
**claim**: Essay2_methodology_full.md:407 称 FLOW null 'Not a zero-fill artifact. Dropping the extensive-margin zeros (held-only, N=231,860) gives β₃=+0.00122 ...(Verified by re-running the triple difference on the held-only subset)',line 27 称 F6 flow 结果 'Robust to dropping the extensive-margin zeros'。实际脚本 test_obs_only.do:28,32 回归的是 `dos`(已被撤回、非 float-immune 的 Δ(ownership_share) 对照结果),且输入面板根本不含 flow 列。
**failure_scenario**: 审稿人以为 F6 纯交易 flow null(β₃=+0.00088)已对剔除 extensive-margin 零稳健。实则 +0.00122 是 dos 结果(全面板 dos=+0.000599),被以 flow 措辞放在 FLOW 结果段落里。dos 恰是 §7.6 自己以 F6 理由否定的度量,文档对 headline shares-based 结论的关键 anti-zero-fill 辩护对其所辩护的结果不成立。
**反驳者1** refuted=False | severity_adjust=Keep major. Mitigation is only rhetorical: line 407 never literally says "flow" (misleading by placement), and the dos held-only sign (+, away from disengagement) makes it plausible flow would agree, but that is exactly the unverified claim at issue. Fix is cheap: add a flow column to a regenerated held-only panel, rerun, and either update the number or relabel the existing one as dos.
  reasoning: Attempted refutation failed; the accusation is confirmed by direct re-execution. (1) test_obs_only.do lines 28/32 regress `dos`, and the file contains no flow regression. (2) output/ownership_c6_panel_obsonly_paired.dta has columns [firm_str, rdate, hgroup, dos, os, cn_lag, shock, us] with N=231,860 and NO `flow` column, so a flow result cannot have been produced from it; it is dated 2026-07-02 03:59, before the F6-rewrite panel (06:40), and the current build_ownership_share_c6_panel.py contains no obsonly-generating code (the held-only robustness is not reproducible from the repo). (3) Decisive: re-running the test_obs_only.do spec in Stata on that dta gives dos β₃ = +0.0012237 (fq gq, N=231,860), matching Essay2_methodology_full.md:407's "+0.00122, N=231,860" digit for digit (the fq gq ig variant gives +0.0012864). So the "Not a zero-fill artifact" defense placed in the §7.6 FLOW results section, and change-log line 27's "Robust to dropping the extensive-margin zeros" attached to the FLOW β₃=+0.00088, are in fact based on the superseded dos outcome that §7.6 itself demotes as not float-immune (F6). No script, log, or panel exists that could have produced a flow held-only result. The failure_scenario is real: a reviewer would read the FLOW null as verified robust to dropping extensive-margin zeros when that check was never run on flow.
**反驳者2** refuted=False | severity_adjust=major → minor. The mislabeling is real (dos result presented as flow verification), but the substantive claim survives when actually run: flow held-only β₃=+0.00138 (vs full-panel +0.00088), same sign, same "further from disengagement" direction; held-only sample fully reproducible from current panel (os>0 re-paired reproduces N=231,860 and +0.001224 exactly). Required fix: rerun held-only on flow, replace +0.00122 with the flow number, and add test_obs_only.do (or its flow successor) to the §14 inventory.
  reasoning: Code claims verified via independent paths: (1) test_obs_only.do:28,32 regresses dos; the input dta (N=231,860) has no flow column (checked with pandas); timestamps show the obsonly panels + do-file (03:58-03:59) predate the F6 flow panel (06:40), so the check could not have used flow. (2) Independent Python within-transformation on the obsonly panel reproduces β₃=+0.001224 on dos — exactly the +0.00122 the doc cites at line 407 in the FLOW section. So the doc-drift is real: refuted=false. However, the predicted failure consequence is overstated: the held-only subset is exactly reconstructible from the current ownership_c6_panel.dta (os>0 + re-pair → N=231,860 identical, dos β₃=+0.001224 identical to 6 decimals), so reproducibility is not lost, and running the missing test — FLOW on the same held-only subset — gives β₃=+0.001379, positive and further from the disengagement prediction than the full-panel +0.00088. The anti-zero-fill defense therefore DOES hold for the flow result it defends; only the cited number/outcome label is wrong. Fix is a one-number doc edit (or rerun test_obs_only.do on flow from the current panel).

## [B9] MAJOR — Russia R1 (it+gt) 两路 cluster VCE 退化:SE=0/缺失,CSV 带 '.' 静默导出、无断言无人诊断
**file**: julia_descriptive/run_russia_headline.do:37  dims=stata,russia
**claim**: run_russia_headline.do R1(absorb(fq gq), vce(cluster firm_n rd_m))的 CGM 两路 cluster VCE 非正半定且高度奇异,reghdfe 返回 se=0、t/p 全缺失;该列仍被 esttab(行 59-64)写入 russia_headline_results.csv,b=-7.41e-06 配 se='.'、p='.',do 文件没有任何 nonmissing-SE 断言(如 assert !missing(_se[us_ru_shock]))。同 absorb 的 R3 与加 ig 的 R2 都有正常 SE,China 全网格同款 spec 报出 p=0.443(07f 引述),故该失败是 Russia 面板特有,非同构行为。
**failure_scenario**: 任何引用 Russia it+gt 列的对比都没有合法的 CRVE 推断;同一面板同一 CGM 机制下 R2(3-pairwise)的 p=0.0853 是正控制'设计能检测 2022 撤资'的唯一 CRVE 支撑,其可靠性应打折并以 RI(p≈0.114)为准。若非正定 VCV 的根源(rd_m 聚类维度病态)同样潜伏在 China headline 机器里,主结果 CRVE 推断也需复核;R1 的失败当前被静默吞掉。
**反驳者1** refuted=False | severity_adjust=维持 major。缺失 SE 被静默写入 headline 结果 CSV、无断言拦截,且正控制论证的 CRVE 支撑因此只剩 R2 单列,RI(p≈0.102)成为 it+gt spec 唯一推断,符合 major 定级;不建议升 critical,因为 reghdfe log 中有显式警告且 RI 作为替代推断已存在,点估计本身(b3)不受影响。
  reasoning: 指控在所有关键点上被独立复核证实。(1) run_russia_headline.do 第 37 行确为 reghdfe dw us_ru us_ru_shock, absorb(fq gq) vce(cluster firm_n rd_m),全文件无 nonmissing-SE 断言。(2) 在当前 c6_panel_russia.dta 上独立重跑 R1(log 20260722021818):reghdfe 发出 'VCV matrix was non-positive semi-definite; adjustment from Cameron, Gelbach & Miller applied' 与 'variance matrix is nonsymmetric or highly singular' 警告,e(V) 全零,se(us_ru_shock)=0,t/p/F 全缺失,b=-7.414427e-06,与指控 evidence 完全一致。(3) output/russia_headline_results.csv 第 3-4 行 it_gt 列 se='.'、p='.',而同 absorb 的 R3 与加 ig 的 R2 均有正常 SE(R2 p=0.0853),失败确为 R1 特有且被静默导出。(4) russia_ri_results.csv 的 b3=-7.414427e-06 与 R1 点估计全精度一致、ri_p=0.1022,RI 确实是该 spec 唯一可用推断。唯一措辞瑕疵:run_ri_russia.py 并非'使用 R1 的 b3'作为输入,而是通过配对差分解析重算出同一系数(算法上与 it+gt spec 等价),数值恰好完全一致;这不改变 failure_scenario 的实质。无任何证据表明代码不存在、被误读或该场景未发生。
**反驳者2** refuted=False | severity_adjust=downgrade: major -> minor
  reasoning: 指控的事实层全部独立复现成立:在当前 c6_panel_russia.dta 上原样复跑 run_russia_headline.do 第 37 行 R1(absorb(fq gq) vce(cluster firm_n rd_m)),reghdfe 报 'VCV non-positive semi-definite; CGM adjustment applied' + 'variance matrix is nonsymmetric or highly singular',e(V) 为全零 3x3 矩阵,se(us_ru)=se(us_ru_shock)=0,F 缺失;output/russia_headline_results.csv 第 3-4 行 it_gt 列 se/p 确为 '.';do 文件无任何 nonmissing-SE 断言;run_ri_russia.py 经 US-NONUS 差分恰好 collapse it+gt FE,russia_ri_results.csv 的 b3=-7.4144e-06 与 R1 点估计逐位一致、ri_p=0.1022,确为该 spec 唯一推断。故 refuted=false。但后果被夸大,三点降级理由:(1) 承重 spec 认定错位——do 文件头部明言与 China headline(run_headline_3pairwise.do)同构的是 3-pairwise 即 R2(it+gt+ig),R1 只是菜单里的轻量列;R2 虽也触发 CGM non-PSD 警告,但特征值调整本是 Cameron-Gelbach-Miller 推荐的标准程序,调整成功后 SE 正常(p=0.0853),不构成'唯一 CRVE 支撑失效';(2) 失败并非无声——CSV 导出的是可见的 '.' 而非错误数字,log 明文打印三条警告,没有任何错误推断被传播,下游文本实际锚定 R2+RI,这是缺 assert 的报告卫生问题,不是结果污染;(3) 传染推测被我的分块诊断证伪——firm-only(se=5.57e-06, p=0.183)、rd_m-only(se=2.95e-06, p=0.014)、交叉 cluster(se=4.77e-06)三块各自完全正常,同 absorb 同 cluster 的 R3 也正常,说明退化是 R1 特定回归元与两路相减的数值巧合,'rd_m 聚类维度病态潜伏在 China 机器'无依据(China 同款 spec 报 p=0.443 正常)。修复成本一行 assert !missing(_se[us_ru_shock]) 加一条脚注即可。诊断脚本:scratchpad/audit_b9_r1.do,log 20260722021843。

## [B10] MAJOR — LP 回归元 ru_lag 随 horizon 时变,h>=1 为 post-treatment 暴露,与 'pre-invasion 2021Q4 暴露' 的声明矛盾
**file**: julia_descriptive/build_russia_lp_panel.py:36  dims=russia
**claim**: build_russia_lp_panel.py 第 36 行取 g.ru_lag(= 该 horizon 季度 t 的 russia_share_lag1q),而 run_russia_lp_test.py 文档串(第 5-6 行)声称回归元是 'ru_lag, as of 2021Q4'(pre-invasion 暴露)。实际上只有 h=0(2022Q1,lag=2021Q4)干净;h>=1 用的是入侵后暴露。
**failure_scenario**: 切断俄罗斯供应链的公司(最强撤资者)在后期 horizon 被重新编码为 ru_lag=0 进入对照组,系统性衰减 h>=1 的 beta;beta 从 h=3 的 -2.01e-4 衰减到 h=7 的 -1.18e-4 可能纯属处理组→对照组迁移的 artifact,且各 horizon 截面构成不同,不能解释为同一 cohort 的冲击响应路径。positive control 唯一 p<0.05 的证据(h=0/1 permutation p=0.042/0.046)中 h=1 已被污染。
**反驳者1** refuted=False | severity_adjust=维持 major。指控完全成立且 failure_scenario 在数据中实际发生。缓和因素仅一点:h=0(2022Q1,lag=2021Q4)截面干净且 perm_p=0.042 仍为负号证据,positive control 未全灭;但 h>=1 的 LP 路径解释和 h=1 的 p<0.05 证据确实不可用,不足以降级。
  reasoning: 亲读当前版本代码:build_russia_lp_panel.py 第 36 行确实 SELECT g.ru_lag(horizon 季度 t 的 russia_share_lag1q),base CTE 只固定 w_base 不固定暴露;run_russia_lp_test.py 文档串声称 'ru_lag, as of 2021Q4' 且按 (firm, report_date) 逐 horizon 取 any_value(ru_lag),文档与实现矛盾属实。duckdb 实测 russia_lp_panel.parquet 逐项复现指控证据:h=0 vs h=7 有 371 家公司 ru_lag 变动、115 家由正归零(占 h=0 时 342 家暴露公司的 34%)、1,038 家新公司进入、n 从 5,466 增到 6,313;russia_lp_test_results.csv 确认 h=0/1 perm_p=0.0417/0.0461、beta 由 h=3 的 -2.01e-4 衰减到 h=7 的 -1.18e-4。唯一可能反证(russia_share_lag1q 对每家公司恒定)被数据否定。failure_scenario 真实:撤资公司后期被重编码为对照组,系 post-treatment conditioning,方向上正是衰减机制;h=1 的暴露(2022Q1)已是入侵后值,确实污染;仅 h=0 干净。无确凿反证,无法证伪。

## [B12] MAJOR — Russia positive control 在项目自身的 design-based 推断标准下未通过 5%:'设计已验证'的推断链条目前缺乏支撑
**file**: julia_descriptive/output/russia_ri_results.csv:2  dims=russia
**claim**: 连续 shock 头条 b3=-7.33e-06 CRVE p=0.085(russia_headline_results.csv),RI p=0.102(russia_ri_results.csv)/0.114(russia_ri_3pairwise.csv);2022Q1-Q2 事件系数在全部 81 个滚动 2 季窗口中 |b3| 排名仅前 27%(实测 russia_event_window_placebo.csv,post-2010 子样本 7.3%)。方法论文档第 460 行的逻辑是'若设计检出已知撤资,China null 才 informative',但按项目用于评判 China 结果的同一 RI/placebo 标准,该前提目前不成立。唯一 p<0.05 的证据是 LP h=0/1 permutation(0.042/0.046),属 8 个相关检验无 multiplicity 校正,且 h>=1 受 post-treatment 污染(见 B10)。
**failure_scenario**: 写论文时若引用 LP perm p≈0.04 作为'positive control 通过'的依据(而 CRVE/RI 均 >0.05),即为选择性引用被污染且未校正多重性的检验;反之若如实报告未通过,则 China null 的 'informative' 定位需降级为 power-limited——两个方向的结论都取决于此,必须在 re-review 中明确裁决并同步文档第 460 行的推断链条。
**反驳者1** refuted=False | severity_adjust=维持 major。该裁决直接决定论文核心定位(China null 是 informative 还是 power-limited),且不利的 placebo rank 结果(27%/7.3%)完全未写入文档,违反项目自身 "claims must match the data" 规范。若坚持降级,最多降到 medium,理由仅限于:文档已三处标注 pending re-review、尚无任何已写下的虚假断言、post-2010 placebo (7.3%) 接近阈值。建议裁决方向:如实写"positive control 方向一致但按预设 5% 标准未通过(RI p≈0.10-0.11)",把 China null 定位降为 power-limited,并把 placebo rank 补进文档;不得单独引用 LP h=0/1 的未校正 p≈0.04 作为通过依据。
  reasoning: 逐项核对后无法证伪该指控,所有数字均与文件精确一致:

1. russia_headline_results.csv 第 2-5 行:3-pairwise b3=-7.33e-06,CRVE p=0.0853;event_2022 的 us_ru_post p=0.0614。均 >0.05,与指控一致。
2. russia_ri_results.csv:b3=-7.414e-06,RI p=0.1023(82 quarters,200k permutations);russia_ri_3pairwise.csv:RI p=0.1142。与指控一致。
3. russia_lp_test_results.csv:8 个 horizon 中仅 h=0 (perm p=0.0417) 和 h=1 (0.0461) 低于 0.05,其余 0.054-0.29。"8 个相关检验无 multiplicity 校正"属实(0.042 过不了任何 8-test 校正)。
4. 我独立重算 russia_event_window_placebo.csv(81 个滚动 2 季窗口):2022Q1-Q2 窗口 b3=-8.96e-05,|b3| 排名 22/81 = 27.2%;post-2010 子样本 4/55 = 7.3%。两个数字与指控分毫不差。run_ri_russia.py 第 124-128 行的代码意图正是这个 fraction,不存在误读。
5. Essay2_methodology_full.md 行号全部准确:line 460 确有"If the design detects those known divestments, the China null becomes 'truly absent'"的条件逻辑;line 643 确标 mixed/fragile pending adversarial re-review;grep 全 repo 确认 27%/7.3% 的 placebo rank 结果未写入任何文档(line 610 只提及该检验存在,未报告其不利结果)。

结论:按项目评判 China 结果所用的同一 RI/placebo 5% 标准,Russia positive control 目前确实未通过,指控的 failure_scenario(两个方向的论文结论都悬于此裁决)真实成立。

两点减轻情节(不构成证伪):(a) 指控说 line 497/612 "未标注缺陷"略有夸大——这两行其实都带 "mixed evidence, pending (adversarial) re-review" 标注,只是未逐项列出 multiplicity/post-treatment 问题;目前没有任何文档断言 positive control 已通过,failure 是前瞻性风险而非已写下的错误陈述。(b) 全样本 27% 排名被 thin-panel 早期季度污染(2003-2009 每季仅 ~800-1000 obs vs 后期 ~12,000,早期窗口 |b3| 达 6e-3 量级),post-2010 的 7.3% 是更公平的基准——但它仍 >5%,且该子样本切分本身也未预注册。
**反驳者2** refuted=False | severity_adjust=minor
  reasoning: All numeric claims verified exactly against the CSVs (CRVE 0.0853, RI 0.1022, 3pairwise 0.1142, LP perm 0.0417/0.0461, placebo rank 22/81=27.2%, post-2010 4/55=7.27%). Independent stress test: recomputed the 81 placebo windows from c6_panel_russia.parquet with a precision-weighted (studentized) statistic to test whether the 27% rank was an artifact of heteroskedastic early windows (2003-05 exposure mass A_t ~0.04 vs ~15-38 post-2010); studentized rank = 19/81 = 23.5%, so the placebo failure is robust, not a metric artifact. BH over the 8 LP horizons gives min q=0.143, confirming nothing survives multiplicity. So the substance stands: the Russia positive control does not pass at 5% under the project's own design-based standards. However the consequence is exaggerated: (a) Essay2_methodology_full.md line 460 sits in '§10 Deferred robustness (planned, NOT done)' and states the positive-control logic only conditionally ('If the design detects...'), so no '设计已验证' claim exists to be unsupported; (b) contrary to the evidence field, lines 497 AND 612 both already carry the 'mixed evidence, pending (adversarial) re-review' flag — the doc nowhere selectively cites LP p≈0.04 as a pass; (c) every output CSV is a correct computation, and the failure_scenario is a prospective writing error that the existing flags guard against. Real residual gap: placebo rank 0.27 written into no document, and the line-460 inference chain (China null informative vs power-limited) still needs explicit adjudication. That is a documentation/decision gap, not a major defect in code or currently asserted claims — downgrade major → minor.

## [B13] MAJOR — F1b LP 的 wild cluster bootstrap 补救没有解决它自己声称的 overlapping-window 序列相关问题
**file**: julia_descriptive/run_audit_f1b_robust.do:33  dims=stata
**claim**: 文件头(行 2-8)声称用 boottest bootcluster(rd_m) 处理 'Overlapping cumulative windows + only 82 quarter-clusters';但 WCB 按 quarter 抽签仍假设 82 个 quarter cluster 之间独立。cum_h = w_{t+h} - w_{t-1} 的重叠窗口使相邻 stamping 季度的误差通过共同的未来聚合成分跨 cluster 相关(fq/gq FE 只吸收 stamping 季度本身的均值),这既不被 CRVE 也不被 bootcluster(rd_m) 的 WCB 处理。firm 维 cluster 只覆盖 firm 内跨期相关,不覆盖跨 firm × 相邻季度相关。
**failure_scenario**: 若 h=1(CRVE p=0.013)或 h=4(p=0.044)在 WCB 下仍 <0.05,团队会引用'经 WCB 稳健'宣称 LP 边际效应成立,而真实 SE 因跨季度重叠相关仍被低估——反保守方向。现有 RI(audit_ri_3pairwise.csv cum4 p=0.073)是更可信的替代,但该 do 文件把错误定位的 WCB 当作正式补救。
**反驳者1** refuted=False | severity_adjust=minor
  reasoning: 代码事实核实为真:run_audit_f1b_robust.do:2-8 头注确把 WCB(行 33 boottest ... bootcluster(rd_m))定位为对 overlapping-window + 82-quarter-cluster 问题的补救,且 build_audit_panel_f1f2f7.py:72-76 确认 cum_h=w_{t+h}-w_prev 是重叠窗口,故"bootcluster(rd_m) 的 WCB 以 quarter 间独立为前提、不修跨季度重叠相关"的计量批评成立,refuted=false。但 failure_scenario 预言的后果(团队引用"经 WCB 稳健"宣称 LP 边际效应成立)被在案记录直接反驳:Essay2_methodology_full.md 行 64-65 把 F1b LP h1/h4 判为 "CRVE invalid here → not significant under RI";行 77 (F3/F9) 明文 "the previously-planned wild cluster bootstrap fails... Randomization inference... is now the arbiter";行 601 inventory 把该 do 文件重新定性为 "CRVE-vs-alternate-FE sensitivity (see also RI)";全 repo 无任何 WCB p 值被引用、无运行 log。团队实际引用自该文件的唯一数字是反显著方向的 CRVE FE-参数化敏感性 (0.013→0.111)。且所有 LP 点估计为正号(与 β₃<0 假设反向),文档行 67 已声明即便显著也只强化 null 结论,故"反保守低估 SE"不会支撑任何团队想主张的结论。剩余问题仅是已被 RI 取代的探索脚本的过时头注(应删注释或整个文件),不是活跃推断路径上的 bug:major 降为 minor。

# ===== CONTESTED (一票证伪/一票存活, 需裁决) =====
## [B1] CRITICAL — 无 as-of 补齐:US 与 NONUS 报告频率混合,机械伪造 US-NONUS 差异
**file**: c:/Users/xl/OneDrive - Universitat Ramón Llull/git/practice/julia_descriptive/03_eom_etl.jl:163-164  dims=etl
**claim**: 整条流水线对基金持仓没有任何 carry-forward / as-of 填充:t 季度的组别总量只包含 report_date 恰好等于该日历季末的基金。03 的过滤条件 `CAST(REPORT_DATE AS DATE) = LAST_DAY(...) AND EXTRACT(MONTH ...) IN (3,6,9,12)` 还把财年季末在 Feb/May/Aug/Nov 报告的基金整体丢弃(注释只声明了 weekend-EOM 一种情况)。04 的 I_ict 直接 `SUM(adj_mv) ... GROUP BY ... report_date`(04:141-153),05 的 nonus_aggregate per_quarter(05:463-472)和 06 的 ict_grouped(06:144-152)同样按裸 report_date 聚合。美国基金受 13F/N-PORT 约束几乎每个日历季末都在场;欧洲 UCITS 法定最低披露频率是半年(大量基金只在 Jun/Dec 出现),二组的组内构成随季度机械变化。
**failure_scenario**: 一只只在 Jun/Dec 报告的卢森堡 UCITS:其持仓在 Mar/Sep 季度从 nonus 分子与分母同时消失。若其组合构成与月度报告者不同(必然,存在 home bias),则 firm 级 w_NONUS 在相邻季度间零交易也会跳动;zero-fill 网格(06)进一步把这些缺席季度补零,Δw_NONUS 出现半年锯齿。任何恰好落在 Mar/Sep vs Jun/Dec 的 GPR shock 实现都会与该机械锯齿相关,triple-diff 的 β₃ 把报告日历差异误读为 US 相对减持/增持。
**反驳者1** refuted=True | severity_adjust=N/A — refuted. If one insisted on keeping a residual note rather than dismissing, it would drop from "critical" to "low" (the real, unabsorbed channel is a third-order firm×season×exposure×shock-calendar interaction that produces no detectable β₃), but the accusation as written is refuted.
  reasoning: I verified the cited code line-by-line and stress-tested the accusation against the output parquet. Result: the factual premises are real but the load-bearing consequence (a mechanically fabricated / biased triple-diff β₃) is refuted, because the reviewer never read the identifying regression and it absorbs exactly the channel alleged.

WHAT IS TRUE (accusation's premises):
1. No carry-forward. Grep over all .jl confirms the only AS-OF logic (02_*.jl) versions Revere company home_region attributes, NOT fund-holdings. 03_eom_etl.jl:161-164 is indeed the sole date filter: WHERE ADJ_MV>0 AND report_date=LAST_DAY(...) AND MONTH IN (3,6,9,12). No forward-fill anywhere. 04:147-152, 05:463-472, 06:144-152 all SUM(adj_mv) GROUP BY raw report_date. Correctly read.
2. A differential Jun/Dec-vs-Mar/Sep sawtooth genuinely exists and is sharply concentrated in NONUS. Querying output/I_ict_panel.parquet: US n_rows Jun/Mar=1.053, Dec/Sep=1.049 (flat, quarterly 13F); NONUS Jun/Mar=1.369, Dec/Sep=1.364; NONUS tot_mv Dec/Sep=1.577. So NONUS loses ~27% of positions and ~37% of Dec value in Mar/Sep — exactly the UCITS semi-annual pattern the reviewer posits. The zero-fill in 06 (COALESCE(i.I_ict,0) at grid_zfilled, then LAG-based delta_w) does propagate this into a firm-group Δw sawtooth. So the reviewer diagnosed a real data feature.

WHY THE ACCUSATION IS NONETHELESS REFUTED (the consequence does not occur; the design is actually correct):
The accusation's mechanism is explicitly GROUP-level: "US基金几乎每个日历季末都在场;欧洲UCITS...半年...二组的组内构成随季度机械变化" biasing β₃. But the canonical identifying spec (07_regression.do:60-62, results in output/07_reghdfe_results.txt) is: reghdfe dw us_cn us_cn_shock, absorb(fq gq), where fq=group(firm_str rd_day)=firm×quarter FE (α_{i,t}) and gq=group(hgroup rd_day)=holder_group×quarter FE (α_{c×t}). The α_{c×t} = holder_group×quarter FE by construction removes ANY variation common to a holder group within a specific quarter — which is precisely the NONUS group-mean seasonal drop each Mar/Sep. The reviewer looked only at the ETL aggregation (03/04/05/06) and inferred contamination without reading the FE stack; the header comment at 07_regression.do:8-9 even documents that US_c×Shock_t is absorbed by α_{c×t}. So the exact "US-NONUS reporting-frequency mix" the accusation invokes is differenced out in identification.

Empirical confirmation of the null: β₃ (us_cn_shock) = +0.000001, se 0.000002 in Spec 1 (β2+β3), identical in Spec 2 (β3-only) and Spec 3 (full saturation), and -0.000000/se 0.000001 under weaker firm+quarter FE (Spec 4). N=462,564. The accusation predicts the sawtooth would be "误读为 US 相对减持/增持" — i.e., a nonzero spurious β₃. Instead β₃ is a tight, sign-stable null across all four FE specs. The claimed mechanical US-NONUS difference does not materialize in the estimate.

Additionally, the "no as-of carry-forward" is not per se a bug: FactSet detailed holdings are point-in-time snapshots, and carry-forward would inject stale/look-ahead positions. The design's chosen answer to cross-frequency mixing is the α_{c×t} absorption, which is a defensible (arguably cleaner) alternative to imputation.

Honest residual caveat (not grounds for the filed critical bug): a firm-SPECIFIC seasonal deviation that survives both fq and gq FE could in principle load on β₃ only if high-china-exposure EU firms had differentially semi-annual NONUS holders AND GPR shocks correlated with quarter-of-year. That is a third-order channel, is not what the accusation describes (it argues group-level frequency mix), and does not manifest given the precise null. So the accusation as filed — group-level reporting-frequency mix mechanically fabricating β₃ — is refuted on both identification grounds (gq FE absorbs it) and empirical grounds (β₃ is null across specs).

Files: julia_descriptive/03_eom_etl.jl:161-164; 04_us_ownership_european.jl:141-153; 05_combine_visualize.jl:447-480; 06_cartesian_grid.jl:250-294; 07_regression.do:37-62; output/07_reghdfe_results.txt; output/I_ict_panel.parquet (empirical sawtooth split).
**反驳者2** refuted=False | severity_adjust=critical → low:机制存在但预言的 β₃ 污染在直接日历安慰剂下为 null(t=0.24),shock 与半年日历正交(corr=0.0004),且主规格 group×quarter FE 已吸收组级锯齿;剩余影响为噪声/衰减与惯例偏离,非伪造效应。
  reasoning: 代码层面指控属实:03_eom_etl.jl:163-164 确为唯一日期过滤(日历月末 + 月份∈{3,6,9,12}),全流水线无 as-of/carry-forward,04/05/06 按裸 report_date 聚合。机制也真实存在:holdings_eom.parquet 上(EU 证券、EQ/AD、2018-2023)NONUS 侧仅 Jun/Dec 报告的基金持有约 13.6% 的 NONUS MV(US 侧 0.02%);2014-2023 组总量 NONUS 在 Jun/Dec 比 Mar/Sep 高 13.8%(基金数)/17.3%(MV),US 侧基本平。但 failure_scenario 预言的核心后果(β₃ 把报告日历误读为 US 相对增减持)被三条独立检验否定:(1) GPR shock 与 Jun/Dec 日历在 82 个季度上的相关仅 0.0004(均值 0.166 vs 0.168),"shock 恰好落在 Mar/Sep vs Jun/Dec" 的相关不存在;(2) 主规格 absorb(fq gq) 的 holder_group×quarter FE 构造性吸收组级锯齿;(3) 在 audit_c6_panel.parquet(231,282 firm-quarter)上以与 07_regression.do 同构的组内差分+季度去均值+双向聚类框架做直接日历安慰剂:US×CN×JunDec 系数 t=0.24(null),horse race 中加入该项后 β₃ 的 t 仅从 0.78 变 0.75;差分后被解释变量按日历月份的均值≈0、std(0.00036-0.00041)无半年模式。另注:Q1 观察到的部分季度大缺口(2017Q3-2019Q2、2023Q3-Q4)主要源于周末 EOM 掉行(如 2019-03-31 为周日,raw 中 March 仅 51% 行为 EOM 日期),这是代码注释已声明的独立已知 caveat,不是半年报告锯齿。故:指控对代码的描述正确(refuted=false),但 critical 定级夸大——实际影响是 NONUS 侧测量噪声(衰减方向)与偏离文献惯例(Koijen-Yogo 式 carry-forward 缺失),不构成对 β₃ 的机械污染。建议降级至 low/minor,作为稳健性备注(加 carry-forward 或限制同频基金重跑一次即可封口)。

## [B4] MAJOR — 04 mcap join 少键(漏 sec_country):跨国多上市实体导致 ownership_ict 行扇出、权重被下游 SUM 双计
**file**: c:/Users/xl/OneDrive - Universitat Ramón Llull/git/practice/julia_descriptive/04_us_ownership_european.jl:261-263  dims=etl
**claim**: marketcap_it 的粒度是 (sec_entity_id, sec_country, report_date)(04:183, 191 两级 GROUP BY 都含 sec_country),但 ownership_ict 的 LEFT JOIN 只用 `ON i.sec_entity_id = m.sec_entity_id AND i.report_date = m.report_date`(04:261-263)。当同一 sec_entity_id 的 primary-equity 行出现在 >1 个 sec_country(06:89-100 的 'Dual-listed' 诊断证明此类实体存在),join 扇出:每条 I_ict 行复制成多行;05 的 us_w_t 对其 `SUM(portfolio_weight_eu)`(05:640-645)直接双计。同时 ownership_share = 整行 I_ict / 单国 mcap 子总,分子分母口径错配。
**failure_scenario**: 一家 GB+NL 双上市实体在 mcap 表有 2 行:其每条 I 行 join 出 2 行输出 → 05 的 us_w_t 把该公司的 US 权重加总为 2 倍 → Δw 与 bucket 份额被夸大,且只对跨国双上市公司(通常为大市值、高国际暴露 → 与 china_share 相关)夸大,产生与暴露相关的系统性偏差。pw sanity 检查(04:273-281)只抽查两个日期的 US,无法排除其他季度/组别。
**反驳者1** refuted=True | severity_adjust=从 major 撤销;若保留仅作为防御性硬化建议(加 (sec_entity_id, report_date) 唯一性 assert),级别 minor/style
  reasoning: 代码引用属实(04:261-263 join 确无 sec_country 键),但指控的 failure_scenario 被数据确凿证伪:(1) marketcap_it.parquet 中 (sec_entity_id, report_date) 重复键数为 0,join 不可能扇出;(2) ownership_ict 行数与 I_ict_panel 完全相等(均为 10,318,106),证明实际零扇出、05 的 SUM 无行可双计;(3) 结构性反证:原始 holdings_eom 中满足 fsym_id=fsym_primary_id AND issue_type='EQ' 的行,没有任何实体(整个样本期)出现在 >1 个 sec_country,故 mcap GROUP BY 中的 sec_country 是冗余键而非扇出源。指控引用的 06:89-100 dual-listed 诊断跑在全量 EOM 行上(含 ADR/二级上市,无 primary-EQ 过滤),与 mcap 表的行集合不同,不能证明 mcap 存在多国实体——这是证据链的关键误用。"分子分母错配"子指控同样不成立:ADR 行除以实体唯一主上市地市值是实体层面 ownership_share 的合理定义,且回归输入 portfolio_weight_eu 的分母不经过 mcap join。唯一残余是防御性建议:可加 assert 断言 mcap 表键唯一,防未来数据源变化,属 minor 硬化而非现存缺陷。
**反驳者2** refuted=False | severity_adjust=major -> trivial/minor (latent grain-mismatch code-smell only; zero actual fanout — sec_country is 1:1 with sec_entity_id in the entire panel, 0 duplicate rows in ownership_ict, US pw_eu sums to exactly 1.0 every quarter; recommend adding sec_country to the join key or a uniqueness assert as defensive hardening, no results affected)
  reasoning: Code text matches the claim: marketcap_it is grouped by (sec_entity_id, sec_country, report_date) (04:170-192) while the ownership_ict LEFT JOIN uses only (sec_entity_id, report_date) (04:261-263), and 05:640-645 SUMs portfolio_weight_eu with no dedup in between. However, the predicted failure is empirically impossible in this dataset. Independent duckdb queries on the actual outputs show: (1) marketcap_it has 1,833,835 (entity, date) cells and ZERO with more than one sec_country; (2) ownership_ict has 10,318,106 rows = exactly 10,318,106 distinct (sec_entity_id, sec_country, investor_country, report_date) keys — zero fanout duplicates; (3) US portfolio_weight_eu sums to exactly 1.0 in EVERY quarter, with per-quarter inflation from hypothetical duplicates = 0 (not just the two sanity-check dates); (4) root cause: across the entire holdings_eom panel (no filters), no sec_entity_id ever appears with more than one sec_country — sec_country is functionally dependent on sec_entity_id (entity-level attribute, not listing country), so the join key is de facto unique. The claim's key evidential premise — that the 06:89-100 dual-listed diagnostic 'proves' multi-country entities exist — is wrong: the diagnostic merely prints a count, and that count is 0 on this data. No fanout, no double-counting, no exposure-correlated bias. What remains is a latent code-smell (join key narrower than table grain, no uniqueness assert), which would only bite if a future data pull changed sec_country semantics.

## [B6] MAJOR — 02 的 ASOF-then-filter 与区间 join 不等价:重叠 validity 区间时把仍被早期行覆盖的公司/边错误置 NULL 并整行丢弃
**file**: c:/Users/xl/OneDrive - Universitat Ramón Llull/git/practice/julia_descriptive/02_china_exposure.jl:211-228  dims=etl
**claim**: rev_co_asof_q(02:211-228)先用 ASOF 选出 start_d<=qend 的最新行,再 `WHERE (h.end_d IS NULL OR h.end_d >= p.qend)` 过滤。rev_co_dedup 只对 (company_id, start_d) 去重,不消除重叠区间:当最新 start_d 的行 end_d<qend、但更早行 [start_d,end_d] 仍覆盖 qend 时,BETWEEN 式区间 join 会保留该公司(代码注释声称二者等价——不成立),ASOF 版直接把公司从该季度的 EU universe 剔除。同一模式在 rev_rel 的 src/tgt as-of(02:295-305, 316-326)把 region 置 NULL,使该边同时从 eu_china_edge(分子)和 eu_any_edge(分母)消失。
**failure_scenario**: 公司有两行:[2003-01-01, 2010-12-31] region=DE 与 restatement 行 [2008-01-01, 2009-06-30]。对 2009-09-30~2010-12-31 各季度,ASOF 选中 2008 行但其 end_d 已过 → WHERE 剔除 → 公司在这些季度退出 EU universe,其持仓行在 05/06 面板中失去 Revere 匹配;同样机制下边的 region=NULL,china_share 的分子分母都被无声削减,且削减与数据商 restatement 相关(非随机)。
**反驳者1** refuted=True | severity_adjust=Downgrade from major to info/nit: replace the 'equivalent to the original BETWEEN-style join' comment at 02:215-219 with a data-contingent statement, and optionally add a cheap assertion (count of BETWEEN-covered but ASOF-dropped probes with non-NULL home_region == 0) to guard future data refreshes. No rebuild of downstream parquets needed on account of B6.
  reasoning: B6's logical premise is real (ASOF-then-filter is not universally equivalent to a BETWEEN interval join; rev_co_dedup at 02:190-211 leaves 38 nested-overlap row pairs across 23 of 703,495 companies), but the claimed failure_scenario was tested directly against the raw Revere data with an exact replication (same sentinel normalization, same NULLS FIRST dedup, genuine DuckDB ASOF JOIN) and does NOT occur. Quarterly: only 94 of 13,432,986 (company,qend) pairs diverge, and all 94 have BETWEEN-assigned home_region = NULL, so none would enter eu_revere_universe_qend (02:486-487 requires home_region IN EU tuple) under either implementation — no firm exits the EU universe in any quarter. Edge side: only 6 of 4,465,327 (endpoint, rel_start) probes diverge, all 6 again with BETWEEN-assigned region NULL, so eu_china_edge (02:360,374) and eu_any_edge (02:417,429), which require non-NULL EU/CN regions, are bitwise identical — china_share numerator and denominator lose nothing. On kept pairs the ASOF pick is provably the same row as the BETWEEN rn=1 pick, so no attribute drift either. The specific alleged harm (firms silently dropped from the EU universe, restatement-correlated erosion of china_share) happens zero times in this data; the only true defect is the code comment overclaiming unconditional equivalence.

## [B11] MAJOR — 2021Q4 陈兵期 anticipation 完全未处理;项目自己的 placebo 显示 2021Q4 窗口比选定的入侵窗口更极端
**file**: julia_descriptive/run_russia_headline.do:49  dims=russia
**claim**: 事件窗定义为 2022m1-2022m6(run_russia_headline.do:49),LP 基线锚定 2021-12-31 的 w(build_russia_lp_panel.py:33)。俄军 2021 年 10-12 月陈兵、美方 11-12 月公开预警,若美国机构在 2021Q4 已抢跑减持,则事件窗漏掉响应、LP 基线 w 已被压低,两者都把 positive control 推向 null。代码与 Essay2_methodology_full.md 均无任何 anticipation 讨论(grep 'anticipat|2021Q4|buildup' 仅命中一处与中国 within-quarter timing 相关的第 432 行)。
**failure_scenario**: positive control 被自身窗口定义截断而显得'边缘显著',可能得出'设计检不出已知的 2022 俄罗斯撤资、China null 不可解释'的错误结论;或反之,若把 2021Q4 归入 pre 期做平行趋势检验会误判 pretrend 失败。至少应做 2021Q4-2022Q2 扩窗与把基线前移到 2021Q3 的稳健性。
**反驳者1** refuted=True | severity_adjust=从 major 降至 minor/documentation:核心 failure_scenario 被数据推翻,剩余合理部分仅为编辑性建议——在 Essay2_methodology_full.md 的 Russia 小节加一段 anticipation 讨论,报告单季分解(2021Q3 +1.24e-05 / 2021Q4 -5.16e-05 排 47.6 百分位)与 3 季扩窗 b3=-7.76e-05(减弱)作为稳健性脚注即可。
  reasoning: 代码引用属实(run_russia_headline.do:49 窗口 2022m1-2022m6;build_russia_lp_panel.py:33 基线 2021-12-31),但指控的唯一数据证据被误读:russia_event_window_placebo.csv 的滚动 2 季窗口以 start_quarter 标记,start=2021-12-31 的窗口覆盖 {2021Q4, 2022Q1},包含入侵季本身,与实际窗口共享 2022Q1,故其"更极端"不是陈兵期抢跑信号。用 c6_panel_russia.parquet 按 placebo 同一闭式公式做单季分解:2021Q3=+1.24e-05(正号)、2021Q4=-5.16e-05、2022Q1=-1.62e-04、2022Q2=-1.45e-05;响应集中在窗口内的 2022Q1(与 LP h=0 的 -1.514e-04 印证),而 2021Q4 的 |b3| 在全部 82 个单季中仅排第 47.6 百分位(中位数级噪声,量级与无事件的 2022Q4/-4.37e-05、2023Q1/+5.98e-05 相当),不支持"2021Q4 已抢跑"。指控要求的扩窗 {2021Q4-2022Q2} 实算 b3=-7.76e-05,弱于选定窗口的 -8.96e-05——并入 2021Q4 是稀释而非增强,"窗口截断把 positive control 推向 null"的 failure 机制被数据反证;LP 基线压低即便按面值也仅为 Q1 效应的 1/3 且属噪声,而 LP h=0..3 perm_p 已达 0.042-0.077,未被推向 null;"2021Q4 归入 pre 期误判 pretrend 失败"同样不会发生。文档确无 anticipation 讨论,但 Essay2_methodology_full.md 已将 Russia positive control 整体标注 mixed evidence/pending re-review,未据 p=0.0614 下任何 B11 担心的结论。
**反驳者2** refuted=False | severity_adjust=major -> minor
  reasoning: 代码事实全部属实(line 49 窗口、line 33 基线、placebo CSV 数值、无 anticipation 讨论),故不判 refuted=true。但用与项目 placebo 相同且经复算验证的 FE-collapse 代数对 c6_panel_russia.parquet 做逐单季分解后,failure_scenario 的三条预言后果全部落空:(1) 2021Q4 单季 b3=-5.16e-05,在 82 个季度的 placebo 分布中绝对值排名 39(47.6% 的季度更极端),纯噪声水平,"2021Q4 已抢跑"不成立;指控引用的窗口对比是重叠伪象——两窗口共享 2022Q1(-1.62e-04),start=2021-12-31 窗口更极端只因实际窗口的第二季 2022Q2≈0(-1.45e-05)比 2021Q4 更弱,不是 Q4 有信号。(2) 指控要求的 2021Q4-2022Q2 扩窗我实际跑了:b3=-7.76e-05,绝对值反而小于选定窗口的 -8.96e-05,即纳入 2021Q4 稀释而非增强,"窗口截断把 positive control 推向 null"方向说反。(3) LP 基线 2021-12-31 被压低的量级至多 -5.2e-05(噪声级),不构成实质偏误;2021Q4 归入 pre 期也不会误判 pretrend。残余问题仅为文档缺一段 anticipation 讨论加单季分解报告,属 minor 文档/呈现缺口,非 major 设计缺陷。

# ===== REFUTED (双票证伪, 已死) =====
## [B5] MAJOR — 06 权重分子分母口径错配:分子 I 跨全部 sec_country 求和,分母只含 EU sec_country
**file**: c:/Users/xl/OneDrive - Universitat Ramón Llull/git/practice/julia_descriptive/06_cartesian_grid.jl:144-152  dims=etl
**claim**: ict_grouped 以 sec_entity_id ∈ eu_entity_universe 过滤后 `SUM(I_ict) GROUP BY sec_entity_id, holder_group, report_date`(06:144-152)——对入选实体把其所有 sec_country 行(含非 EU 上市线,如 sec_country='US' 的存托/关联证券行)全部计入分子;而分母 total_holdings_eu 来自 04:230,只含 EU sec_country。凡 EU-universe 实体同时挂有非 EU sec_country 行,其 portfolio_weight_eu 分子被系统性放大、组内权重之和 > 1。
**failure_scenario**: 瑞士实体同一 sec_entity_id 挂 CH 普通股(EU tuple 内)和一条 sec_country='US' 的证券行:US 投资者经该 US 线的持仓进入 w 分子但不进入 US 组的 EU 分母 → w_US 相对 w_NONUS 被放大,放大幅度随 ADR 使用强度变化,而 ADR 使用与公司国际化/中国暴露正相关 → 直接污染 US×CN 交互项的横截面识别。
**反驳者1** refuted=True | severity_adjust=refuted — not a bug; at most a defensive-assertion suggestion (add invariant check that sec_country is constant per sec_entity_id), severity none/info
  reasoning: The accusation's literal code reading is correct (06:144-152 GROUP BY omits sec_country) but its premise is false in the data. Verified with duckdb on current parquets: (1) holdings_eom.parquet has ZERO sec_entity_id with more than one distinct sec_country — sec_country is an entity-level issuer-country attribute, so the failure scenario (same entity with a CH line and a sec_country='US' depositary line) never occurs; (2) all issue_type='AD' (ADR) rows of EU-universe entities carry EU sec_country (GB/NL/LU/IE/CH/...), none 'US', so US investors' ADR holdings enter numerator and EU denominator consistently; (3) in I_ict_panel, EU-universe entities have exactly 0 non-EU rows and 0.0 non-EU I_ict mass (vs 1.91e14 EU mass); (4) in the shipped merged_us_eu_zero_filled.parquet, SUM(portfolio_weight_eu) within every (holder_group × quarter) cell equals exactly 1.0 for all 199 group-quarters, zero cells above 1.0001 — the predicted symptom (weight sums > 1) does not exist. Hence the universe filter is an exact equivalent of the EU sec_country filter under the data's schema invariant, as the Patch 3 comment intends. Only optional hardening: add an assert in 06 that sec_country is unique per sec_entity_id to protect against future source changes.
**反驳者2** refuted=True | severity_adjust=n/a (refuted); 若坚持保留该条目, 最多降为 info 级"代码可读性/防御性"备注, 无任何数值后果
  reasoning: 代码字面事实与指控一致(06:144-152 的 ict_grouped 确实没有 sec_country 过滤, 只按 sec_entity_id ∈ eu_entity_universe 过滤; 04:230 的分母确实 EU-restricted), 但指控的核心前提——"EU-universe 实体同时挂有非 EU sec_country 行"——在数据中是空集, 因此"口径错配"根本不存在。用 duckdb 对实际 parquet 做了两层最小验证: (1) 完整复刻 06 的 eu_entity_universe(从 holdings_eom, 无 issue_type 过滤, 12,743 实体)后, 在 I_ict_panel.parquet 中这些实体的非 EU sec_country 持仓金额为 0(US 组和 NONUS 组均为 0 美元, 0 个受影响实体); 逐 (holder_group, quarter) 复刻 ict_grouped/country_total_grouped 的组内权重和, as-coded 与 EU-only 分子两种口径下均恒等于 1.000, 通胀量 avg=max=0, 受污染 cell 数为 0。(2) 结构性原因: 在全量 holdings_eom(1.74 亿 EQ 行 + 352 万 AD 行, 64,183 个实体)中, 没有任何一个 sec_entity_id 出现过多于 1 个 distinct sec_country——sec_country 是实体级属性(发行人属地), 不是上市地/证券级属性; EU-universe 实体的全部 ADR 行(issue_type='AD')的 sec_country 都是 GB/NL/LU/IE/CH/FR/DE 等 EU tuple 内值, 不存在 sec_country='US' 的存托行。指控的 failure_scenario(瑞士实体挂 sec_country='US' 的 ADR 线 → w_US 分子放大 → 污染 US×CN 交互识别)在该数据 schema 下不可能发生: 分子的实体过滤与分母的 sec_country 过滤在数据中选出完全相同的美元集合, Patch 3 的写法与重施 EU sec_country 过滤严格等价(这正是 Patch 3 注释所依赖的性质)。指控声称的"系统性放大、组内权重之和>1"经直接测量为假。验证脚本: scratchpad/b5_refute.py, b5_refute2.py。

# ===== UNVERIFIED (0票, 待 Opus 补跑) =====
## [B14] MAJOR — Tail-dummy 与 post_invasion 规格:处理季度仅 2-6 个,cluster-t p 值在 few-treated-cluster 下无效,且 tail 菜单没有任何 RI
**file**: julia_descriptive/run_tail_3pairwise.do:35
**claim**: tail 冲击只在 z>k 的季度取 1:经复算,k=1.645/2/3 的处理季度分别为 6/4/2(共 82 个)。us_cn_tail 的识别变异集中在 2-6 个 quarter cluster 上,vce(cluster firm_n rd_m) 的 t(81) p 值在 few-treated-cluster 设定下严重失真(MacKinnon-Webb),k=3 时 β₃ 本质上是 2 个季度的对照差。run_ri_* 系列只覆盖连续 shock,tail 菜单没有 permutation 检验。run_russia_headline.do:49-54 的 us_ru_post 同样只有 2 个处理季度(CSV p=0.0614),虽有 run_ri_russia.py 的滚动窗 placebo 补救,但 CSV 里的 cluster-t p 仍是可被引用的工件。
**failure_scenario**: 会议表把 tail 菜单当作'null 在极端冲击下依然稳健'的证据——但 k≥2 的列既无功效也无有效推断,p=0.44/0.83 传达的'精确 null'是假象;反向风险是若某个 tail 列偶然给出 p<0.05(few-treated-cluster 下过度拒绝方向),会被当成真实的尾部效应。tail 列应配 shock-relabeling RI(基础设施已有)或明示 p 不可解释。

## [B15] MAJOR — The only significant ladder cell (firm-level level-w tilt, +8.6e-5**) has no confounder strategy and its FE-saturated neighbor answers a different estimand
**file**: c:/Users/xl/OneDrive - Universitat Ramón Llull/git/practice/julia_descriptive/run_ladder_2x2_cells.do:5-7
**claim**: Step 2 的 cross-sectional-tilt 结论依赖 M3(level w, fq+gq FE):us_cn = +8.6e-5 带星。该系数包含所有与中国暴露相关的时不变公司属性 — 规模、跨国性、指数成员、ADR 可得性 — ladder 全程零公司控制;US 机构对大型跨国公司的已知偏好(机械上更多中国供应链链接)构成完整替代解释。相邻 M4 列加 firm×group FE 后符号翻转(−2.1e-5, ns),但 M4 不是 tilt 的稳健性检验:吸收 μ_{i,g} 后 level 回归只由组内 lagged CN 的*变化*识别,是响应问题而非横截面 tilt。因此没有任何 ladder 列干净回答'US 投资者是否回避高 CN 公司',但 2x2 slide 表把它们并列为可比 cell。
**failure_scenario**: slide 读者(或 advisor)看到 ladder 中唯一带星的 cell,把 'US 投资者对中国暴露公司的 tilt' 当作地缘政治事实。实际上 +8.6e-5 与 mega-cap tilt 不可区分;M3 与 M4 之间的符号翻转是 estimand 变化被当作规格变化呈现,诱发'fragile but suggestive'叙事,而两个数字都不回答该问题。

## [B16] MAJOR — Country-level CN_c is an equal-weighted average over a country-varying selected subset while the outcome w_c aggregates all firms
**file**: c:/Users/xl/OneDrive - Universitat Ramón Llull/git/practice/julia_descriptive/build_country_panel.py:47-74
**claim**: Step 1 的回归元与结果建立在不匹配的总体上。cty_cn 只对暴露非缺失的公司平均 china_share(AVG 忽略 NULL),即 Revere-matched-with-active-links 子集,其覆盖率跨国差异巨大(GB 32.78% unmatched vs DE 11.6%,doc limitation 12);它还对 out-of-span(pre-IPO/退市)公司求均值。cty_w 则对全部公司(含 unmatched)SUM(COALESCE(portfolio_weight_eu,0))。CN_{c,t-1} 的跨国变异混淆了真实暴露差异与 Revere 覆盖构成及 dead-firm carryover,回归元描述的公司集合不同于结果分配资金的集合。给 advisor 只标了 equal-vs-holdings 加权选择,总体错配与覆盖结构未标。
**failure_scenario**: GB — 占 country-pair 权重 60.85% — 三分之一发行人缺席 CN_c 均值但完整出现在 w_c。若 unmatched GB 公司更小/更本土(真实 CN 更低),measured CN_GB 相对 DE/FR 被非随机高估,Step-1 us_cn 系数(country col4 −0.059)部分反映 'US 对覆盖好 vs 覆盖差市场的差异配置'。ladder 的 country-vs-firm 符号反转可能是该聚合 artifact 而非真实 Simpson 型结构,slide 叙事无法区分。

## [B17] MAJOR — CN_{t-1} is a time-varying moderator endogenous to the tension history; no baseline-fixed-exposure robustness exists
**file**: c:/Users/xl/OneDrive - Universitat Ramón Llull/git/practice/julia_descriptive/Essay2_methodology_full.md:179-195
**claim**: 所有规格(headline 与三级 ladder)使用滞后一季、随 Revere 持续更新的 china_share。一季滞后使 CN 对创新冲击 S_t predetermined(AR(1) 下 E[S_t|CN_{t-1}]≈0,β₃ 点识别成立),但 CN_{t-1} 是过去紧张*水平*的结果:公司在持续压力下主动脱钩。estimand 因此漂移为'对内生选择保持中国暴露的公司的效应',2018-2023 升级的幸存者是被选择的非响应者。§9 的 16 条 limitation 无一提及,也没有把暴露固定在升级前基线(如 2015-2017 CN,Autor-Dorn 式固定份额)的稳健性——在现有网格上只需加一列。
**failure_scenario**: 若真实 H2.1 效应存在且通过推动暴露公司切断中国联系起作用(公司恰因 US 投资者威胁退出而 de-risk),则 2020-2023 剩余的高 CN_{t-1} 公司不成比例地是股东不在乎的公司,滚动 moderator 上的 β₃ 被衰减向零,即便撤离压力真实存在。论文的'robust null'可能部分是 moderator-churn artifact——当前 battery 无法排除、文档未披露的情形。

## [B18] MAJOR — BIL_{c,t} is a contemporaneous, plausibly post-treatment control: for the key mechanism countries, US-country bilateral friction IS the China channel
**file**: c:/Users/xl/OneDrive - Universitat Ramón Llull/git/practice/julia_descriptive/build_country_panel.py:20-22
**claim**: ladder 的 BIL 控制是*同期*月度 USA|c GPR 水平的季度均值,其 US 交互进入所有主样本 ladder 列(M1-M4)并加入 run_ddd_nofe_bil.do Stage C2 的 DDD。声明目的(吸收'general US-Europe frictions',Greenland 例)假设 BIL 独立于 US-China 处理。但在中国机制最强的国家,双边摩擦正是由中国政策引起:美国就 ASML 出口管制施压荷兰、就 Huawei 5G 施压德英,直接记录在 USA|NL、USA|DE、USA|GB 的新闻 GPR 中。控制 US×BIL_t 会 partial out 因果通道的一部分(mediator 而非 confounder),恰好在承载效应的公司上衰减 us_cn 与 β₃。BIL 还未滞后而 CN 已滞后,破坏项目在别处(§7.3b)坚持的 predetermined-regressor timing。存在缓解(同样本 no-BIL cell 已算),故不评 critical,但无文件披露 mediator 风险,而 with-BIL 列是指定 main。
**failure_scenario**: 2019-2023:升级的美中紧张通过 ASML 争端抬高 USA|Netherlands GPR;US 特有的从 NL 上市中国暴露半导体设备股撤出被归因于 us_bil 而非 us_cn(×S)。slide 上'result robust to bilateral-relations control'的措辞会把 over-controlled 系数当作更干净的估计报告,而偏误方向恰指向论文已报告的 null。

## [B19] MINOR — c6_panel_russia.parquet 与 russia_gpr_quarterly.parquet 无生成脚本、无一致性校验:RI 与 Stata 读不同文件,存在静默失同步风险
**file**: julia_descriptive/run_ri_russia.py:30
**claim**: run_ri_russia.py:30 与 run_ri_russia_3pairwise.py:21 读 c6_panel_russia.parquet,run_russia_headline.do 读 build_russia_c6_panel.py 产出的 .dta;但仓库内 grep 不到任何写 c6_panel_russia.parquet 的代码。russia_gpr_quarterly.parquet(06_russia_grid.jl:53 的必需输入)同样无生成脚本——build_russia_shock.py 只写 russia_shock_monthly.csv(且被任何脚本读取为零),文档第 590 行也只归属到 monthly CSV。monthly→quarterly 转换(季末月选择、列名、日期约定)是 ad hoc 完成的;不同于 China 侧(build_sagg_panel.py:84-88 硬断言 stamped shock 等于 monthly 文件的季末残差),没有任何校验把 parquet 中的 shock_us_ru 对齐已提交的 monthly AR(1) 输出。
**failure_scenario**: 重跑管线(例如落地 02 文件头声明的 2026-07-21 NULLS-FIRST 修复)只会更新 .dta 与 merged parquet,RI 继续读旧 c6_panel_russia.parquet,CRVE 与 RI 的 p 值将来自不同样本而无任何报错;06_russia_grid.jl 无法从零复现。若 ad hoc 转换 stamped 了错误月份子集、GPR 水平而非 AR(1) 残差、或部分季度用了陈旧拟合,licensing China null 的 Russia β₃ 就建立在不可审计输入上——完全日期错配会被 build_russia_c6_panel.py 的 n>0 断言抓住,但任何部分/数值级错误静默通过。

## [B20] MINOR — investor_country NULL 约定跨 builder 不一致:04 无声整体剔除(无量级诊断),ownership-share 面板却归入 NONUS
**file**: c:/Users/xl/OneDrive - Universitat Ramón Llull/git/practice/julia_descriptive/04_us_ownership_european.jl:150
**claim**: I_ict 构建 `WHERE ... investor_country IS NOT NULL` 把 ISO_COUNTRY 缺失的基金持仓同时从 US、NONUS 两组及所有分母中剔除,且没有输出被剔除行数/adj_mv 占比的诊断;06:147 的 `CASE WHEN ...='US' THEN 'US' ELSE 'NONUS'` 本会把 NULL 落入 NONUS,构成两处不一致的 NULL 约定(当前因 04 已提前过滤未触发)。而 build_ownership_share_panel.py 直接从 holdings_eom 建组份额,同样的 CASE(line 111)且 BASE_WHERE(lines 75, 106-119)无 IS NOT NULL 过滤,缺失注册地的 holder 被并入 NONUS 的 held/flow 分子。因此 F6 flow 结果与主 w 结果使用不同的 NONUS holder 集合,尽管 ownership_c6_panel 被描述为 'structurally identical to c6_panel.dta'(build_ownership_share_c6_panel.py:6)。
**failure_scenario**: 若 ISO_COUNTRY 缺失集中于离岸注册载体(常见),w 侧 NONUS 分母被系统性低估且剔除量随年份/数据批次漂移引入趋势,无任何输出可事后核查量级;flow 侧则相反,unknown-domicile holder 的交易加载到 NONUS flow,只污染 shares-based 检验的对照侧。当 w-based null 与 flow-based null 被作为同一 US-vs-NONUS 对比的互补证据呈现时,两个结果静默比较不同的对照组;若未知注册地 holder 多为 US 管理的离岸载体,flow 检验的 US-NONUS 差异被衰减。

## [B21] MINOR — Russia 同构复制重命名不完整:manifest 用 China step 名写 meta,CN_SRC/n_cn 标签泄入 Russia 输出
**file**: julia_descriptive/06_russia_grid.jl:309
**claim**: 06_russia_grid.jl 调用 write_manifest("06_merged_us_eu_zero_filled", merged_path...) 而 merged_path 是 merged_us_ru_zero_filled.parquet,Russia 面板的 sidecar .meta.json 记录了 China 的 step 名(从 06_cartesian_grid.jl:298 复制未改)。同文件 console 标签也未改名('06_cartesian_grid.jl v2' banner 行 63、'Shock^{US-CN}' 行 494)。02_russia_exposure.jl 文件头(2-8 行)承诺'every identifier is renamed china->russia',但第 376 行 Russia path 2 仍标 'CN_SRC'(注释 368 行写 'CN company is source'),386 行汇总列名 n_cn 实为俄方对手数,548-552 行内部表名 firm_quarter_cn,640 行打印 'CN relation count'。
**failure_scenario**: 管线一致性检查或人工审计按 meta.json step 名列举时,看到两个 artifact 声称 step 06_merged_us_eu_zero_filled 而行数不同,误判 China 面板被重建/更改,或按 China-step 预期校验 Russia parquet;读 02_russia_edge_path_summary.csv 的审计者会以为文件混入 China 边计数,或按 path='RU_SRC' 过滤而静默得到空集。与文件头显式保证矛盾,降低'同构复制'声明的可信度;无数值结果受影响。

## [B22] MINOR — No anticipation placebo: identifying assumption of the DDD has no stated testable version, and the natural future-shock test (US·CN·S_{t+1}) was never run
**file**: c:/Users/xl/OneDrive - Universitat Ramón Llull/git/practice/julia_descriptive/Essay2_methodology_full.md:75
**claim**: 文档从未明确陈述 triple-diff 的识别假设(条件于 FE,US-vs-NONUS 对高 CN 公司的差异再配置在无冲击时对 S_t 均值独立)及其可检验版本。整个 timing battery — F1a lead-flow Δw_{t+1}~S_t、§7.3b S_{t-1}、sagg 分布滞后 — 把 lead/lag 放在结果或滞后放在冲击上,每个方向都在检验*延迟响应*。对创新冲击的 parallel-trends 式证伪是反方向:Δw_t ~ US·CN·S_{t+1}(差异是否在新闻*之前*移动?),同时兼作跨季度 anticipation 检验。grep 确认全管线无 shock-lead 回归元;§9 limitation 3 只声明 *within*-quarter anticipation。
**failure_scenario**: 2018 关税轮等升级在生效前数季度公告;在公告时建仓的 US 机构会产生负的 US·CN·S_{t+1} 系数配 null 的 S_t 与 S_{t-1} — 正是观察到的模式。当前 battery 无法区分'无撤离'与'撤离被完全前置',论文的 null 结论静默排除第二种解读,而所需检验只是现有代码的一列。

## [B23] MINOR — RI 自由置换假设 shock 可交换,但 stamped 季度冲击的 lag-1 自相关 = 0.271,仅 s_agg 脚本披露并做了 circular-shift 变体
**file**: julia_descriptive/run_ri_3pairwise.py:93
**claim**: run_ri_3pairwise.py:93、run_ri_russia.py:57-58、run_ri_shocklag.py:47-49 都对 82 个季度冲击做自由置换(rng.permutation),隐含 S_t 可交换/白噪声。用当前 c6_panel.dta 复算 stamped shock 的 lag-1 自相关 = 0.271,在 n=82、白噪声 SE≈0.110 下约 2.5 个标准误——AR(1) 预白化并不完全。只有 run_ri_sagg.py(行 8-12, 69-72)披露此问题(s_agg 自相关 0.357)并补了 circular-shift p;headline/3-pairwise/Russia/shocklag 的 RI 均无 block 或 shift 变体。
**failure_scenario**: 对接近边际的统计量(cum4 RI p=0.073、Russia RI p≈0.10-0.11),违反可交换性的自由置换 p 可能向任一方向偏移;若以'RI 确认'作为最终推断语言,应至少给 headline 系列补 circular-shift/block 置换,否则边际结论的稳健性存疑。

## [B24] MINOR — run_ri_russia.py 的 RI 在 it+gt 下计算,却把 3-pairwise 的 Stata CRVE 结果当作参照打印
**file**: julia_descriptive/run_ri_russia.py:74
**claim**: run_ri_russia.py 的连续冲击 RI 用季度内 demean 的闭式解(行 36-49),精确对应 it+gt(R1)而非 it+gt+ig;行 74 却打印 '(Stata CRVE 3-pairwise reference: b3=-7.327e-06, p=0.085)'。RI 的 b3_obs=-7.4144e-06 与 R1 的 it+gt 点估计逐位一致(russia_headline_results.csv it_gt 列 b=-7.41e-06),不等于被引用的 3-pairwise -7.33e-06。
**failure_scenario**: 把 run_ri_russia.py 的 RI p=0.102 与 3-pairwise CRVE p=0.085 并排引用会构成规格错配的'确认';结合 B9(R1 的 CRVE 已退化),这个 RI 其实是 it+gt 规格下唯一合法的推断,标签错误会让它被安到错误的列上。应把参照行改为 R1 并注明其 CRVE 不可用。


# ===== ROUND 2 (Opus 补验 B14-B24 + B6/B10/B13, 2026-07-22) =====

## [B14] status=CONFIRMED
**Opus反驳者1** refuted=False severity_adjust=
  Attempted to refute B14 but every factual component checks out. (1) Cited code exists as described: run_tail_3pairwise.do line 35 uses vce(cluster firm_n rd_m); lines 30-31 build the tail dummy z>k and its interaction. (2) Data confirms treated-quarter counts exactly: replicating the .do construction on output/audit_c6_panel.parquet gives n=82 quarters and treated quarters 6/4/2 for k=1.645/2.0/3.0 — identical to the claim, so identification is on 2-6 quarter clusters, the MacKinnon-Webb few-treated-cluster regime where the cluster-t t(81) p-value is invalid. (3) grep -i tail run_ri*.py returns nothing — the tail menu genuinely has no permutation/RI test while continuous-shock scripts do. (4) run_russia_headline.do:49-54 post_invasion spans 2022m1-2022m6 = exactly 2 quarters with the same cluster-t vce, matching the '2 treated quarters' claim; Russia has run_ri_russia.py rolling-window placebo as the claim concedes, but the CRVE p stays a citable artifact. The failure scenario (tail columns read as 'robust null under extreme shocks' despite no power and invalid inference at k>=2, plus reverse over-rejection risk) is a legitimate textbook concern, correctly characterized, not a misread. No confirming evidence for refutation found; MAJOR severity is appropriate (governs a robustness-claim interpretation, fix infrastructure exists, but supplementary to the RI-backed continuous-shock headline, so not critical).
**Opus反驳者2** refuted=False severity_adjust=MAJOR->MINOR
  Every load-bearing fact in B14 reproduces exactly. I independently recomputed the tail construction from audit_c6_panel.parquet using the same recipe as run_tail_3pairwise.do:19-34 (sigma over 82 distinct quarters, one-sided z>k) and got 82 quarters with 6/4/2 treated quarters for k=1.645/2/3 — matching the claim to the unit. The CRVE setup (vce(cluster firm_n rd_m), tail interaction nonzero in only 2-6 of 82 quarter-clusters) is real; reported tail p-values in 07e_firmgroup_tail.csv are 0.9457/0.4391/0.8262 (verdict's 0.44/0.83 correct); no run_ri_*.py references 'tail' (tail menu genuinely lacks RI); Russia us_ru_post has 2 treated quarters with CSV p=0.0614. So the code is exactly as described -> refuted=false.\n\nBut the failure consequence is overstated, so severity drops. (1) The MacKinnon-Webb few-treated-cluster defect is ANTI-CONSERVATIVE (over-rejection, SEs too small, p too small). Every tail column FAILS to reject with large p (0.44-0.94). Valid/more-conservative inference (wild-cluster bootstrap or RI) would only widen intervals and raise p — the reported non-rejection is reinforced, never overturned. The defect cannot fabricate a genuine-looking rejection from a true null; it can only do the reverse, which did not happen. (2) The scenario's 'reverse risk' of a spurious p<0.05 taken as a real tail effect did NOT materialize — the most extreme k=3 column is p=0.83 with se=3.9e-5 (7x the baseline us_cn SE). That inflated SE is the honest low-power signature already visible in the reported statistic, so a reader sees a wide band (8.6e-6 ± 3.9e-5), not a 'precise null.' (3) The only near-marginal column is Russia p=0.0614, which still does not reject at 5% and already carries the run_ri_russia.py rolling-window placebo remedy the verdict itself acknowledges; it is also a positive-control, not the tail-menu anchor (run_tail_3pairwise.do:35) that headers B14.\n\nWhat survives is a legitimate but narrower framing/reporting caveat: label the k>=2 tail columns 'no DETECTABLE tail effect' (a 2-quarter DDD contrast with near-zero power) rather than a 'tight/precise null,' and optionally bolt the existing shock-relabeling RI onto the tail menu. That is a wording/robustness-hygiene fix, not a computational error and not a threat to any reported conclusion. Hence MAJOR -> MINOR.

## [B15] status=CONFIRMED
**Opus反驳者1** refuted=False severity_adjust=none (keep MAJOR)
  Verified both technical prongs against the current code and could not disprove either.

ESTIMAND-CHANGE PRONG (run_firm_ladder.do:34-38, confirmed): M3 = reghdfe w us_cn us_bil, absorb(fq gq), with fq=group(firm_str rd_day)=firm×date and gq=group(hgroup rd_day)=holdergroup×date. us_cn is identified from the US-vs-NONUS weight differential within firm-date — a cross-sectional tilt. M4 adds ig=group(firm_str hgroup)=firm×holdergroup FE, which absorbs precisely the time-invariant US-vs-NONUS tilt M3 relies on, leaving us_cn identified only off within-firm-group time variation in cn_lag. So M4 answers a different estimand (a response question), is NOT a robustness check of the M3 cross-sectional tilt, and the sign flip (+8.6e-5 -> -2.1e-5) is an estimand change presented as a spec change. Claim is econometrically correct.

CONFOUNDER-GAP PRONG (run_ladder_2x2_cells.do & run_firm_ladder.do): the only control anywhere in the ladder is us_bil = us*bil_us_c. There is no us×size / us×ADR / us×index interaction. Firm×date FE (fq) absorbs firm-date LEVELS but not a DIFFERENTIAL US-vs-NONUS size preference, which is exactly the identifying variation for us_cn. Hence a US-vs-NONUS mega-cap differential tilt (mega-caps high-CN) is observationally indistinguishable from geopolitical China-avoidance in M3. Firm×date FE does not close this gap.

The claim's phrase '系数包含所有时不变公司属性' slightly overstates (firm×date FE does absorb firm-date levels), but the confound it describes operates through the interaction/differential that survives the FE, so this is loose wording, not a factual error that overturns the indictment.

No concrete disproof found: code citations are accurate (file/lines match), the M3/M4 estimand distinction is real, and the confounder strategy is genuinely absent in the code. This is a valid, material identification/framing critique of the sole starred ladder cell, not a misread. Severity MAJOR is defensible for a slide result being over-interpreted; no grounds to downgrade on 'not serious'. Evidence: run_firm_ladder.do:16-18 (fq/gq/ig definitions), :34-38 (M3/M4 specs), run_ladder_2x2_cells.do:5-7 (comment recording firm col4=+8.6e-5**, col5=-2.1e-5 ns).
**Opus反驳者2** refuted=False severity_adjust=major -> minor
  B15 code facts fully CONFIRMED, so not refuted. I re-ran the exact regressions on firm_ladder_panel.dta: M3 (reghdfe w us_cn us_bil, absorb(fq gq)) on LEVEL w gives us_cn=+8.6e-5, se=4.3e-5, t=2.03, p=0.046 (one star, exactly as cited); M4 adding ig=group(firm_str hgroup) flips to us_cn=-2.1e-5, p=0.637. The panel carries no firm size/multinationality/index/ADR controls (cols: firm_str, sec_country, hgroup, rdate, w, dw, cn_lag, bil_us_c, us), confirming 'zero firm controls throughout the ladder.' The econometric critique is valid: with only firm×quarter FE, us_cn is a US-vs-NONUS differential onto which a US-specific large-cap/ADR preference correlated with CN can load (unaddressed), and M4's firm×group FE genuinely changes the estimand to within-firm-group time variation (a response question), not a cross-sectional-tilt robustness check.

However the load-bearing failure_scenario is overstated, justifying a downgrade. (1) The methodology doc EXPLICITLY discloses the estimand difference at Essay2_methodology_full.md:299 ('Adding mu_{i,g} absorbs any time-invariant US-vs-NONUS tilt toward each firm; beta2, beta3 then identify off within-(firm,group) time variation'), so the M3->M4 move is NOT presented as a plain spec change; the doc is transparent that they answer different questions. (2) The paper's evidentiary framing (lines 5, 41, 557) is a ROBUST NULL; +8.6e-5 is never cited as a tilt/geopolitical fact anywhere in the doc body — it appears only in a slide add-on (run_ladder_2x2_cells.do), Step-2 of an explicitly 'no shock yet' descriptive ladder. (3) The magnitude is economically trivial: w SD among US rows is 1.5e-3, so a 1-SD move in cn_lag (0.07) shifts the US-NONUS gap by ~6e-6 = 0.004 SD of w, at a marginal p=0.046. (4) Prior B2 refutation already showed the cell survives in-span (+1.44e-4, t=1.99) but stays marginal. The genuine residual is a slide-hygiene gap: the 2x2 table sets M3(starred) next to M4(ns) as 'FE x controls' cells without annotating that M4 is a different estimand — a one-line annotation fix, not a MAJOR analytical defect. Note also the verdict file's own exclusion list drops figure/slide scripts and advisor-flagged selections, which this cell borders on. Net: refuted=false, downgrade major -> minor.

## [B16] status=CONFIRMED
**Opus反驳者1** refuted=False severity_adjust=
  B16 is NOT refuted. The indicted code exists and is read correctly. build_country_panel.py:56 computes cn_c = AVG(cn), and AVG ignores NULL, so CN_c is an equal-weighted mean over only the firms with non-missing china_share; lines 57-58 even track COUNT(cn) vs COUNT(*), the code author's own acknowledgment that this subset is smaller than the full set. Line 64 sums COALESCE(portfolio_weight_eu,0) over ALL firms for w_c. So the regressor population (matched firms) differs from the outcome population (all firms), exactly as B16 states.\n\nThe failure_scenario occurs in the data (duckdb, merged_us_eu_zero_filled.parquet): china_share is NULL for 2,073,242 of 2,548,600 rows (81%) — genuinely NULL, not zero-filled, so the 'selected subset' is real. At firm-quarter level, coverage varies by country (GB 84.2% unmatched, DE 78.4%, CH 73.4%, GR 88.0%). Within GB, matched share drifts from 2.6% (2003) to 39.1% (2023), so the CN_c mismeasurement is strongly time-varying and survives country×group FE into the within-estimator identifying us_cn. None of the refutation grounds hold (code present, not misread, scenario present in data, behavior is a genuine population mismatch), so refuted=false.\n\nCalibration notes (do not overturn the verdict): (1) the scenario's 'GB = 60.85% of country-pair weight' is overstated ~2x — actual GB share of total portfolio weight is 28.35% (GB is still dominant). (2) The specific static 'US allocates differentially to well-covered markets' mechanism named for col4/M4 is partly absorbed by the cg (country×group) FE in run_country_ladder.do:45. However the time-varying coverage drift I measured is NOT absorbed, so the underlying concern is preserved and, if anything, the data reinforce its severity. I therefore do not recommend a downgrade; MAJOR is defensible.
**Opus反驳者2** refuted=False severity_adjust=MAJOR -> MINOR (downgrade): code is exactly as described (real, material population mismatch: 33% of w_c weight from firms excluded from CN_c), but the predicted consequence is not borne out. (1) GB's measured CN is the LOWEST of GB/DE/FR (0.0111 vs 0.0269/0.0168), so the "CN_GB overstated relative to DE/FR" mechanism is directionally false. (2) us_cn is insignificant under all three CN constructions and all specs (p 0.35-0.99, 16 clusters). (3) The negative country M1 coefficient the accusation calls an aggregation artifact PERSISTS under the selection-removing holdings-weighted construction (-0.139, still null), so it is not manufactured by the selective averaging. Construction/disclosure gap is real; inferential threat is not.
  B16 has two layers. Layer 1 (code description) is literally CORRECT: build_country_panel.py:54-60 computes cty_cn as AVG(china_share) over firms with non-missing exposure (COUNT(cn) ignores NULL), while cty_w (l.62-66) sums COALESCE(portfolio_weight_eu,0) over ALL firms. I verified against output/merged_us_eu_zero_filled.parquet: china_share is NULL for 81.35% of firm-quarters, and ~33% of the portfolio-weight mass in w_c comes from NULL-china_share firms that are excluded from CN_c (US book coverage 64.8%, NONUS 68.1%). So the population mismatch is real and material. That is why refuted=FALSE — the code is exactly as the accusation describes, not a misread.

Layer 2 (the failure_scenario / consequence) does NOT hold, and this is the reason to downgrade severity:
(a) The scenario's central mechanism — "measured CN_GB is non-randomly OVERSTATED relative to DE/FR" — is directionally false in the data. GB's measured CN (0.0111) is already the LOWEST of GB/DE/FR (DE 0.0269, FR 0.0168). Under the extreme null->0 correction GB stays lowest. It is not overstated relative to DE/FR; if anything understated.
(b) The scenario's inferential claim — that the negative country us_cn (~-0.059) and the "country-vs-firm sign reversal" are aggregation artifacts — is refuted by re-running the exact run_country_ladder spec (M1/M2/M4, cluster ctry rd_q) under three CN constructions: current equal-weight-over-nonnull, null->0 equal-weight over all firms, and holdings-weighted over all firms. In EVERY construction and EVERY spec us_cn is statistically insignificant (p 0.35-0.99; only 16 country clusters, which the .do itself flags as descriptive). Critically, the negative M1 coefficient the accusation blames on the aggregation PERSISTS under the holdings-weighted construction (M1 = -0.139), which removes both the equal-weight-over-selected-subset issue and the population mismatch. So the selective aggregation does not manufacture the negative sign; it survives the clean construction (still null). The aggregation choice is not load-bearing for any reported inference.

Net: the construction inelegance/documentation gap is genuine (worth a robustness column and honest disclosure of the covered-subset averaging), but it does not threaten any inferential claim, because the Step-1 country estimate is a construction-robust null and the GB-overstatement direction is false. This is 'code as described but consequence exaggerated/immune' -> refuted=false with a MAJOR->MINOR downgrade. Evidence reproducible via duckdb queries on merged_us_eu_zero_filled.parquet and the reghdfe loop over the three CN constructions.

## [B17] status=CONFIRMED
**Opus反驳者1** refuted=False severity_adjust=
  我以"该指控错误"为立场逐项核对,但三个可证伪的事实主张全部成立,找不到确凿反证:

(1) 代码确实用滚动、滞后一季的 CN moderator。Essay2_methodology_full.md:179-195 明确 CN_{i,t}=R^CN/R 后滞后一季;line 185 "Home-region classification is point-in-time (time-versioned)" 即逐季时变;lag 由 build panel 的 `LAG(china_share,1) OVER (PARTITION BY sec_entity_id, holder_group ORDER BY quarter_end)`(methodology 171 行)实现;HIGH cutoff(line 195)也用 CN_{i,t−1}>0.0476 的滚动值。指控对代码的引用准确,无误读。

(2) §9 的 16 条 limitation(line 428-445 全读)无一提及"CN moderator 因过去紧张而内生/幸存者选择/moderator churn"。最接近的第 15 条(line 444)讲的是 NONUS 不是干净对照、共同 de-risking 对 OUTCOME(配置)的影响,属于结果侧共同趋势问题,与"高 CN 公司随时间被内生淘汰、剩者是股东不在乎者"的 MODERATOR 选择问题不同。第 6 条(estimand shifts)只讲 full-grid vs risk-set 样本切换,非时间性幸存。全文再 grep `surviv|retain|churn|moderator|endogenous.*exposure` 亦无该披露(retain 命中的是 build_spell_boundary 的另一个 bug 与 risk-set,不相关)。

(3) 全 julia_descriptive 目录 grep `fixed.?base|baseline.?exposur|shift.?share|autor|dorn|pre.?escal|2015|frozen|time.?invariant CN` 找不到任何把暴露固定在升级前基线(Autor-Dorn 式固定份额/2015-2017 CN)的稳健性规格;§10 deferred robustness(line 449-462)清单里也没有该项。指控"该稳健性不存在、文档未披露"属实。

指控的逻辑也自洽:它自己承认一季滞后使 β₃ 对 shock 点识别成立(E[S_t|CN_{t-1}]≈0),提出的是不同层面的 estimand-drift/外部效度问题——高 CN 公司随 2018-2023 升级被内生脱钩淘汰,剩者非随机,β₃ 被衰减向零。这是真实、未披露的稳健性缺口,衰减方向与文档已披露的多条 attenuation 限制(11/13/14)一致,但针对的是 moderator 而非它们针对的对象,不被现有 battery 覆盖。

因此无法证伪。severity 维持 MAJOR:与同类 B18(over-control/mediator、可缓解但未披露、衰减方向)同级,属于真实的识别/estimand 威胁+缺失稳健性+未披露,而非编码 bug 或"我觉得不严重"。证据:Essay2_methodology_full.md:179-195, 428-445, 449-462;全目录 grep 无 fixed-baseline exposure 规格。
**Opus反驳者2** refuted=False severity_adjust=MAJOR->MINOR (downgrade)
  B17 (MAJOR): claims all specs use a time-varying, lag-1, rolling-updated china_share (CN_{t-1}); that CN_{t-1} is endogenous to the tension history (firms de-couple under sustained pressure); that no baseline-fixed-exposure (Autor-Dorn shift-share) robustness column exists; and that the "robust null" on β₃ may be a moderator-churn/survivorship artifact biased toward zero.

WHAT IS TRUE (code as described, so NOT refuted on the facts):
- CN is time-varying lag-1: confirmed. 07_regression.do:48-49 builds us_cn=us*cn_lag and us_cn_shock=us*cn_lag*shock from china_share_lag1q; §4.2 (methodology:181) defines CN_{i,t}=R^CN/R then lagged one quarter, point-in-time home-region.
- No baseline-fixed-exposure robustness: confirmed. grep across *.jl/*.do/*.py/*.md finds no 2015-2017/first-quarter/initial/static-share exposure spec; the only "baseline" hits are no-FE OLS baselines and the AR(1) US sanity fit, unrelated.

WHY THE CONSEQUENCE IS EXAGGERATED / LARGELY IMMUNE (so refuted=false + DOWNGRADE):
(1) The headline is NOT a HIGH-CN subsample gate. It uses cn_lag CONTINUOUSLY. In merged_us_eu_zero_filled.parquet, china_share_lag1q has 3,170 distinct values on [0,1] (mean 0.0142; 69,254 positive rows). 07_regression.do absorbs α_{i,t}(firm×quarter)+α_{c×t}(group×quarter); the sole identifying contrast is US-vs-NONUS investor Δw WITHIN the same firm-quarter, scaled by continuous exposure. Firm-level dynamic selection into "remaining high-CN" does not gate the sample — it only reweights a continuous regressor — so the "surviving high-CN firms are selected non-responders → β₃ attenuated to zero" mechanism has no sample-gate to act through.

(2) The behavioral premise (de-risking under pressure) is empirically FALSE in this data. Cohort test on firm_quarter_china_exposure.parquet, firms HIGH (>0.0476) in 2018, tracked 2018→2023: china_share falls (avg 0.347→0.147, frac-still-high 0.877→0.556), which superficially looks like decoupling. But the decomposition shows the NUMERATOR of actual China links RISES (n_cn_total avg 3.38→5.86), while the DENOMINATOR (n_total_links) rises faster (33.6→48.2). The share decline is a coverage/denominator artifact (Revere cataloguing more total edges), not firms cutting China ties. Absolute China linkage grows through the escalation window. With no real exit-of-exposure, the survivorship-attenuation channel (US investors pushing exit, only apathetic-shareholder firms remaining exposed) has no fuel. Aggregate frac_high also rises 2018→2023 (0.031→0.043), driven by universe expansion.

RESIDUAL VALID KERNEL (keeps it a real, but MINOR, note): conditioning β₃ on a moderator that can itself respond to prior shocks is a defensible thing to disclose, and a baseline-fixed / first-observed-CN (shift-share) robustness column is genuinely absent and cheap to add. That is a disclosure/robustness gap, not a demonstrated bias. The MAJOR framing ("robust null may be a moderator-churn artifact") is not supported: the design is within-firm US-vs-NONUS on a continuous regressor, and the required decoupling behavior does not occur in the data.

Reproducible evidence (all via duckdb on julia_descriptive/output/): (a) continuity of china_share_lag1q; (b) 2018-HIGH cohort share-decline vs numerator-rise decomposition; (c) yearly frac_high rising; plus grep confirming no baseline-fixed spec and 07_regression.do confirming continuous within-firm FE identification.

## [B18] status=CONTESTED
**Opus反驳者1** refuted=True severity_adjust=major -> minor (if retained: documentation/timing nit only; same-sample no-BIL β₃=1.86e-06 vs with-BIL 1.77e-06, 0.05 SE — result demonstrably robust, "robust to bilateral control" wording is accurate)
  Adversarial verdict on B18 (MAJOR): I verified every code-level fact B18 cites and they are ALL accurate (not a misread) — BIL is contemporaneous (build_country_panel.py:20-22,100-101 lag only cn_c/w_c, never bil_us_c), CN is lagged, us_bil enters all main ladder columns M1-M4 (run_country_ladder.do:33,39,42,45) and Stage C2 of run_ddd_nofe_bil.do:62, the 16-country with-BIL cell is the designated main (full-28 no-BIL is a "coverage check"), and the docstrings frame BIL only as a confounder-guard without disclosing mediator risk. The timing asymmetry (BIL contemporaneous vs CN lagged) is real.

However, refuted=true rests on the "该场景在数据中不出现" criterion: I ran the same-sample DDD (the mitigation cell B18 itself references but did not quantify). Results (N=429,136 both): C1 no-BIL β₃=1.8617e-06 (se 1.765e-06, p=0.295); C2 +US×BIL β₃=1.7685e-06 (se 1.781e-06, p=0.324). The BIL control shifts β₃ by only −9.3e-08 = −0.053 SE, the us_cn main term is essentially unchanged (5.78e-06→5.89e-06), and us_bil is itself insignificant (p=0.218). The NULL is already present in the same-sample no-BIL cell. Therefore the failure_scenario — that the contemporaneous BIL mediator over-controls, attenuates β₃/us_cn toward the reported null, and makes the slide's "robust to bilateral-relations control" wording misleading — does not materialize. The estimate is genuinely robust (0.05 SE), us_bil does not steal us_cn variation, and the "robust" claim is accurate. The MAJOR harm that anchors B18 is empirically absent. Residual (undisclosed mediator-candidate + timing asymmetry in a main-designated spec) is a legitimate but at-most-minor documentation/timing nit, fully defused by the same-sample no-BIL cell showing identical results. Evidence: build_country_panel.py:100-101,119-124; run_country_ladder.do:24,30-53; run_ddd_nofe_bil.do:55,58-63; Stata run 20260722101213.log C1 vs C2.
**Opus反驳者2** refuted=False severity_adjust=MAJOR -> MINOR (downgrade; code is as described but predicted consequence is empirically immune)
  B18 has two parts: (A) a code-description claim, and (B) a predicted consequence. I checked both.

PART A — code IS as described. In build_country_panel.py:20-22 BIL_{c,t} is the quarterly mean of the contemporaneous monthly USA|c AI-GPR level, with NO lag applied (line 100 lags only cn_c → cn_c_lag; bil_us_c stays contemporaneous). In run_country_ladder.do lines 24, 33/39/42/45 the interaction us_bil = us*bil_us_c enters M1-M4, and the with-BIL 16-country sample is designated main (line 30 `keep if !missing(bil_us_c)`), with the no-BIL cell only as an M2-full28 coverage check. So the timing asymmetry (CN lagged, BIL contemporaneous) and the "us_bil in main specs" descriptions are factually correct.

PART B — the predicted consequence does NOT materialize (immune). I re-ran the full ladder WITH and WITHOUT us_bil on the identical 16-country/2,610-obs main sample. The us_cn coefficient is essentially invariant to the BIL control in every spec: M1 −0.05147 vs −0.05323, M2 +0.00322 vs +0.00302, M3 −0.05889 vs −0.06408, M4 +0.00222 vs +0.00228. Maximum change is 8% (M3) and the direction of movement is inconsistent — in M2/M4 including BIL moves us_cn slightly AWAY from zero, the opposite of the claimed attenuation-toward-null. Significance is unchanged (null in all 8 regressions). Decisively, us_bil is itself insignificant and near-zero everywhere (p = .53/.79/.52/.77), so it absorbs essentially no outcome variance and cannot be partialling out any China channel. The failure_scenario's specific mechanism — "US pull-out gets attributed to us_bil not us_cn, the over-controlled coefficient reported as cleaner, bias toward the reported null" — is empirically falsified: us_cn is not reattributed to us_bil, and the null is present with AND without the control (so BIL is not what produces the null).

VERDICT: refuted=false (the code genuinely is a contemporaneous, unlagged, main-spec control as the claim states — the description is accurate and the un-disclosed mediator/timing-asymmetry point has a valid kernel worth a one-line disclosure), but the predicted quantitative harm is immune/exaggerated: dropping the control moves β₃ by ≤8%, never flips significance, and moves it away from zero as often as toward it. The claim itself already concedes the same-sample no-BIL mitigation exists and rated it MAJOR not critical; my re-derivation shows that mitigation fully neutralizes the concern. Downgrade MAJOR → MINOR/near-INFO.

Evidence reproducible via: build_country_panel.py (construction), run_country_ladder.do lines 24-48 (spec), and the with/without re-run in scratchpad/b18_test.do on output/country_panel.dta.

## [B19] status=CONFIRMED
**Opus反驳者1** refuted=False severity_adjust=none (MINOR stands)
  B19 verified as accurate; cannot refute. (1) run_ri_russia.py:30 and run_ri_russia_3pairwise.py:21 read c6_panel_russia.parquet, while run_russia_headline.do:13 reads c6_panel_russia.dta produced by build_russia_c6_panel.py:31,113 (to_stata only, no parquet) — RI and Stata genuinely read different files. (2) Repo-wide grep finds NO code that writes c6_panel_russia.parquet (only reads); the only russia-side to_parquet is build_russia_lp_panel.py:50 for a different file. The parquet exists in output/ with no committed generator. (3) russia_gpr_quarterly.parquet is a required INPUT to 06_russia_grid.jl (checked :56, STEP_INPUTS :60, read :279/424/473/527) and is written by no script — confirmed no generation script. (4) build_russia_shock.py:42 writes only russia_shock_monthly.csv, which is read by zero other scripts; the monthly→quarterly conversion has no committed code and no assert aligning parquet shock_us_ru to the monthly AR(1) residual, whereas the China side (build_sagg_panel.py) does carry such an assert. Every sub-claim checks out and the silent-desync failure scenario (rebuilding .dta while RI reads a stale parquet → CRVE and RI p-values from different samples with no error) is mechanically possible. This is a genuine MINOR reproducibility/auditability gap, not a misread or nonexistent code. No severity adjustment needed; MINOR is appropriate.
**Opus反驳者2** refuted=False severity_adjust=downgrade MINOR -> trivial/info (latent reproducibility-hygiene gap; no active contamination)
  CODE FACTS ARE AS ACCUSED (not refutable): repo-wide grep confirms no script writes c6_panel_russia.parquet (build_russia_c6_panel.py writes only the .dta at line 31/113; the two RI scripts only read the parquet) and no script writes russia_gpr_quarterly.parquet (only read at 06_russia_grid.jl:53); build_russia_shock.py writes only russia_shock_monthly.csv, which zero scripts read; and there is no assert aligning the parquet shock to the monthly AR(1) output. So refuted=false.

BUT every harmful consequence in the failure_scenario is empirically falsified against the current on-disk artifacts, via an independent path (direct file comparison):
(1) c6_panel_russia.dta vs .parquet are BITWISE IDENTICAL — 462,564 rows, max abs diff on dw/ru_lag/shock = 0.000e+00, quarter-shock series identical. RI (parquet) and Stata (dta) therefore currently read the SAME sample; the alleged 'CRVE and RI p-values come from different samples with no error' does not occur.
(2) russia_gpr_quarterly.parquet reproduces the committed monthly AR(1) output EXACTLY: all 265 quarter-ends merge (0 missing), max abs diff on shock = 0.0 and on GPR level = 0.0. The three specific feared errors are each disproven — correct quarter-end month subset (all 265 present), the value is the AR(1) RESIDUAL not the level (corr with level only 0.454), and the 2022Q1 Ukraine peak (+10.52) is intact. The 'unauditable input licensing the China null' is in fact fully auditable and I audited it: it matches the committed monthly AR(1) series to 0.0.

Timestamp chain (monthly CSV 08:53 -> gpr_quarterly 08:56 -> merged 08:57 -> dta 09:00 -> parquet 09:02, all same session) is consistent with an ad-hoc dta->parquet and csv->quarterly conversion that is currently in sync.

The only surviving issue is a PROSPECTIVE reproducibility/hygiene gap: the parquet-generation and monthly->quarterly steps are unscripted and lack a consistency assert, so a future pipeline re-run could silently desync. That is latent, not active, and the item is already MINOR. The failure_scenario dramatizes active contamination that is currently absent, so severity should drop from MINOR to trivial/info; fix = add a dta->parquet generation step and a one-line assert comparing parquet shock_us_ru to the monthly CSV quarter-end residuals. Verification scripts: scratchpad/b19_check.py, scratchpad/b19_gpr2.py.

## [B20] status=CONFIRMED
**Opus反驳者1** refuted=False severity_adjust=minor -> trivial/negligible (disclosure hygiene only; 8 bps of NONUS MV)
  All code claims in B20 are accurate and correctly read: 04_us_ownership_european.jl:150 has `AND investor_country IS NOT NULL` dropping NULL-domicile holdings from both groups with NO row/adj_mv diagnostic (grep confirms none near the filter); 06_cartesian_grid.jl:147 uses `CASE WHEN investor_country='US' THEN 'US' ELSE 'NONUS'` which would route NULL to NONUS but is untriggered because 04 pre-filters (exactly the "two inconsistent conventions" described); build_ownership_share_panel.py:111 CASE + BASE_WHERE (line 75, grp lines 106-119) has no investor_country filter so NULL-domicile holders fold into NONUS; build_ownership_share_c6_panel.py:6 claims "structurally identical to c6_panel.dta" while its F6 flow outcome inherits the NULL->NONUS convention the w-based c6_panel.dta does not. The failure_scenario also genuinely occurs in the data (duckdb on holdings_eom.parquet): full panel has 469,774 NULL-investor_country rows (0.25% of rows, 0.025% of MV); within the flow-panel EU primary-EQ universe there are 76,721 NULL rows (0.185%) that ARE folded into the flow-side NONUS but dropped from the w-side NONUS, so the w-based and flow-based tests do compare non-identical NONUS control groups, and no output lets a reviewer audit the dropped magnitude. Therefore I cannot set refuted=true: the code exists, is not misread, the behavior is a genuine cross-builder inconsistency, and the scenario materializes. The ONLY mitigating factor is magnitude — the folded contamination is just 0.082% (8 bps) of NONUS MV and 0.044% of total MV, so any attenuation of the US-NONUS flow difference is negligible. Per the task rules, magnitude is a severity argument not a refutation, so refuted=false; but since B20 is already MINOR and the effect is trivially small (real residual = missing dropped-row diagnostic in 04 + imprecise "structurally identical" docstring, i.e. disclosure hygiene, not a result-changing bug), I suggest downgrading toward trivial/negligible. Evidence: 04_us_ownership_european.jl:150, 06_cartesian_grid.jl:147, build_ownership_share_panel.py:75/111, build_ownership_share_c6_panel.py:6; duckdb queries on output/holdings_eom.parquet (NONUS NULL-domicile MV share = 0.0008228).
**Opus反驳者2** refuted=False severity_adjust=MINOR -> trivial/nit (downgrade)
  Code matches the claim exactly, so B20 is not fabricated: 04_us_ownership_european.jl:150 filters `investor_country IS NOT NULL` (dropping NULL from both groups + denominators with no dropped-row diagnostic), while build_ownership_share_panel.py:75 (BASE_WHERE) has no such filter and its CASE ... ELSE 'NONUS' (lines 111/118) folds NULL-domicile holders into NONUS. The two builders genuinely use inconsistent NULL conventions, and the panel's diagnostics cover shares_out missingness, not investor_country NULL — so the 'no diagnostic' point is also true. Hence refuted=false. However the failure_scenario's material consequences are quantitatively impossible at the measured magnitude. On holdings_eom.parquet (41.4M-row flow universe: EQ, EU sec_country, fsym_primary, adj_holding>0), NULL investor_country = 0.185% of rows, 0.040% of shares held, 0.044% of adj_mv. w-side (04, EQ+AD/EU) drops 0.184% of rows / 0.043% of MV. Contamination of the NONUS flow numerator = 0.063%. Year-by-year MV share is essentially flat (max 0.34% in 2023; row-wise rises to 2.88% in 2023 but MV-negligible), so the alleged year/batch-drifting trend does not exist in MV terms. NULL holders are predominantly missing-metadata rows (entity_type also NaN) plus small mutual funds — no support for the 'US-managed offshore vehicle' hypothesis that drives the attenuation story. A 0.04–0.06% perturbation cannot systematically understate the NONUS denominator, introduce a trend, or attenuate a US-vs-NONUS null in any detectable way. The finding is a legitimate code-hygiene / convention-consistency nit (worth a one-line diagnostic), but its predicted effect on results is immaterial, so severity should drop from MINOR to trivial/nit.

## [B21] status=CONFIRMED
**Opus反驳者1** refuted=False severity_adjust=none (MINOR is correct)
  B21 (Russia isomorphic-copy incomplete rename / stale China labels) is fully corroborated. Every code reference exists and is read correctly:

CODE (verified against current files):
- 06_russia_grid.jl:288 sets merged_path = "merged_us_ru_zero_filled.parquet"; :309 calls write_manifest("06_merged_us_eu_zero_filled", merged_path). This step-name string is copied verbatim from 06_cartesian_grid.jl:298 (identical literal) — unchanged for the Russia run. Confirmed.
- 06_russia_grid.jl:63 banner "06_cartesian_grid.jl v2" — confirmed. :494 console label "Shock^{US-CN}" — confirmed.
- 02_russia_exposure.jl header lines 2-8 explicitly promise "every identifier is renamed china->russia / cn->ru so no column silently holds Russia counts under a China-labeled name." Confirmed.
- :368 comment "Path 2: CN company is source"; :376 'CN_SRC' AS path while :378 WHERE src_region='RU' (so the CN_SRC label is really RU-source). Confirmed.
- :386 COUNT(DISTINCT ru_company_id) AS n_cn — a Russia count under a China-labeled column, directly contradicting the header promise. Confirmed.
- Internal table firm_quarter_cn created at :541 and joined at :604 (the block spanning the claim's cited 548-552 column aliases n_ru_*). Confirmed. :640 println "Distribution of CN relation count". Confirmed. (Bonus stale label at :664 comment "mean China share and mean CN relation count".)

DATA (duckdb/cat on output/):
- Both sidecars record the SAME step: merged_us_eu_zero_filled.parquet.meta.json and merged_us_ru_zero_filled.parquet.meta.json BOTH have "step":"06_merged_us_eu_zero_filled" (different out_path, different sha256). The step-name collision the claim describes is real in the persisted meta.
- 02_russia_edge_path_summary.csv literally has header "n_cn" and path values EU_SRC / CN_SRC (no RU_SRC). So the failure scenario is exactly realized: an auditor filtering path='RU_SRC' gets an empty set, and n_cn holds Russia counterparty counts (EU_SRC n_cn=905, CN_SRC n_cn=219).

The 02_firm_quarter_russia_exposure step name IS correctly renamed, confirming the rename was only partial — precisely what B21 asserts.

Only inaccuracy: the failure narrative speculates the two grid artifacts have "行数不同" (different row counts); in current data both are row_count=2548600 (identical). But that is an incidental detail of the narrative, not the core defect — the step-name collision (two distinct artifacts, same step, different sha256/path) is real regardless of row count, so it does not refute the finding.

No refutation angle holds. The claim's only imprecision is calling "06_merged_us_eu_zero_filled" a "China step name" when the literal reads us_eu (the shared EU-securities universe); substantively it is the main-run step name copied unchanged, which is the point. Severity MINOR is correct and appropriate — the claim itself states no numerical results are affected, which the data confirms (this is a naming/consistency/auditability defect, not a computational one). No downgrade warranted; MINOR is already the floor.
**Opus反驳者2** refuted=False severity_adjust=MINOR -> TRIVIAL (cosmetic/labeling only; flagship failure prong empirically immune)
  B21 的代码描述逐条属实,不可 refute。已复核:

事实层(全部命中,与指控一致):
- 06_russia_grid.jl:309 确实调用 write_manifest("06_merged_us_eu_zero_filled", merged_path...),而 merged_path 是 Russia 文件;00_setup.jl:180-189 确认 write_manifest 把 step_name 原样写入 .meta.json 的 "step" 字段。
- banner 行 63 仍打印 '06_cartesian_grid.jl v2';行 494 仍打印 Shock^{US-CN}。
- 02_russia_exposure.jl 文件头 2-8 行显式承诺 "every identifier is renamed china->russia",但仍残留:368 注释 "CN company is source"、376 'CN_SRC' AS path(WHERE 实为 src_region='RU')、386 汇总列 n_cn、541/604 内部表名 firm_quarter_cn、640 打印 "CN relation count"、664 注释 "mean China share and mean CN relation count"。代码确实就是指控所述那样。

后果层(关键:flagship failure_scenario 被证伪/免疫):
1. failure_scenario 的旗舰后果是"两个 artifact 声称 step 06_merged_us_eu_zero_filled 而【行数不同】→ 误判 China 面板被重建/更改"。实测两个 sidecar 的 row_count 完全相同(EU 与 RU 均为 2,548,600),前提"行数不同"不成立,该误判触发条件不存在。且两个 .meta.json 的 out_path 字段分别精确指向 merged_us_eu_zero_filled.parquet / merged_us_ru_zero_filled.parquet,sidecar 文件本身也各自躺在对应 parquet 旁——文件位置+out_path 双重消歧,按 step 名列举也能立即区分。
2. 仅剩的真实后果:审计者按 path='RU_SRC' 过滤 02_russia_edge_path_summary.csv 会得空集(实测该 CSV 只有 EU_SRC/CN_SRC 两行)。这是人工混淆,纯标签问题。
3. 指控自身承认"无数值结果受影响",复核确认下游列 548-553 已正确改名为 n_ru_*,数值口径正确(俄方对手数),只是外层标签/表名/打印文案 stale。

结论:代码属实(refuted=false),但预言的主要危害(审计因行数不同而误判 rebuild)在实际产物上被免疫,唯一存活后果为纯 cosmetic 人工混淆、零数值影响。严重度应从 MINOR 降至 TRIVIAL/NIT。证据可复现:cat 两个 .meta.json 与 02_russia_edge_path_summary.csv,grep 02_russia_exposure.jl 的 CN_SRC/n_cn/firm_quarter_cn。

## [B22] status=CONFIRMED
**Opus反驳者1** refuted=False severity_adjust=keep minor (no change) — valid missing-placebo + unstated-assumption gap; partial mitigation exists (limitations 1 and 3, line 424 AR(1)-residual note) but the specific S_{t+1} test and the explicit identifying assumption are genuinely absent
  B22 (MINOR) is NOT refuted; its load-bearing facts all hold against the current files.

Verified evidence:
(1) Full grep of julia_descriptive for any future/lead shock regressor (s_t+1, s_lead, shock.lead, s_next, future.shock, s_fwd, .shift(-1) on shock) returns ZERO estimation specs — only Essay2_methodology_full.md:432, the within-quarter anticipation limitation. So the natural anticipation placebo Δw_t ~ US·CN·S_{t+1} was genuinely never run.
(2) The entire timing battery is backward/delayed-response: F1a lead-flow is an OUTCOME lead, dw_lead1=LEAD(delta_w,1) (build_audit_panel_f1f2f7.py:60) regressed on US·CN·S_t (run_audit_f1f2f7.do:37-40); §7.3b is a shock LAG S_{t-1} (Essay2_methodology_full.md:344,350; run_ri_shocklag.py); sagg is a distributed lag (run_ri_sagg.py). None is a shock lead. This matches the indictment exactly.
(3) §9 limitation 3 (Essay2_methodology_full.md:432) discloses ONLY within-quarter anticipation ("removes t+1 leakage but not within-quarter anticipation"); none of the 16 limitations proposes the cross-quarter US·CN·S_{t+1} test.
(4) §6 Identification (lines 278-291) writes the estimating equation and states what β₃ identifies (∂³Δw/∂US∂CN∂S) but never states a testable conditional-mean/parallel-trends orthogonality assumption, and never proposes an anticipation falsification.
(5) The cited anchor file:line (Essay2_methodology_full.md:75, the F1 finding) is read correctly — it is precisely the delayed-response resolution (lead-flow + LP IRF), confirming the indictment's framing.

The failure_scenario (pre-announced 2018 tariff escalation → US institutions front-run at announcement → negative US·CN·S_{t+1} with null S_t and S_{t-1}) is a plausible, unexcluded alternative; the required test is literally one added column, and it was not run. I found no misread code, no place the test exists, and no data contradiction, so I cannot falsify the indictment.

Since this is about a missing placebo + an unstated (but implicit) identifying assumption — a hypothetical alternative interpretation, not a wrong number — there is no parquet value to check; the grep over the whole pipeline is the decisive evidence.

Note on severity: it is already correctly labeled MINOR and I do NOT recommend downgrading. One phrase in the indictment ('null conclusion silently excludes the second reading') is mildly overstated — limitation 1 (line 430) explicitly says 'We cannot reject H2.1 or its opposite,' and line 424 notes S_t is an AR(1) residual so S_t/S_{t+1} need not correlate — but the two hard factual claims (no shock-lead regression anywhere; no explicit testable identifying assumption) are both true, so the core stands. Valid, correctly-scoped MINOR methodological gap.
**Opus反驳者2** refuted=False severity_adjust=MINOR -> NIT (downgrade): real but immaterial gap
  The factual core of B22 is correct and NOT refuted. grep across all .py/.do/.jl in julia_descriptive/ confirms no shock-lead (US·CN·S_{t+1}) regression exists; build_audit_panel_f1f2f7.py:60-65 only leads the OUTCOME (dw_lead1, w_l1..w_l4) and lags the shock (gpr_lag), and the F1a 'lead-flow' is Δw_{t+1}~S_t, not the anticipation cell. §9 limitation 3 (methodology:432) covers only within-quarter anticipation, never cross-quarter front-running, and no explicit DDD identifying assumption / testable version is stated. All as the indictment says. HOWEVER the failure_scenario's feared consequence does not survive being run. I built the exact missing column on merged_us_eu_zero_filled.parquet (231,282 balanced US/NONUS firm-quarter pairs) in the pairwise-differenced form that reproduces the it+gt β₃, validated by reproducing the published headline β₃=+1.2779e-6 (doc: +1.28e-6). The anticipation test dw~cn·S_{t+1} gives β₃=-6.02e-7, p=0.74 (t=-0.33): a clean null, one order of magnitude smaller than the headline and swamped by its SE. The sign scatters (+ at t, - at t-1, - at t+1), pure noise around zero. The reviewer's worry that the null 'silently rules out fully-front-run exit' is therefore empirically false: running the one-column test yields the same null as S_t and S_{t-1}, so there is no hidden anticipatory US-differential exit. (Also, S is an AR(1) innovation, so S_{t+1} is near-unforecastable from t by construction, making an anticipatory coefficient mechanically ~0.) Verdict: a genuine documentation/completeness gap (keep as a NIT, not refuted) whose claimed material harm is absent — downgrade from MINOR.

## [B23] status=CONFIRMED
**Opus反驳者1** refuted=False severity_adjust=keep minor (already lowest tier)
  未能证伪。B23 的每一项事实指控都经亲自核对属实,行为确实发生,推荐的修复也是合理的方法学要求。

代码核对(当前版本,逐行):
- run_ri_3pairwise.py:93 —— `Sp = rng.permutation(Svec)`,对 82 个季度冲击自由置换。属实。
- run_ri_russia.py:57-58 —— while 循环内 `perms = np.array([rng.permutation(S) for _ in range(b)])`(实际在 56-58 行),自由置换 S。属实。
- run_ri_shocklag.py:47-49 —— 同样 `rng.permutation(S)`(第 49 行)。属实。
- run_ri_sagg.py:8-12 的 docstring 明确披露 corr(s_agg_t, s_agg_{t-1})=0.357 并说明自由置换是近似;69-72 行补了 circular-shift 变体(np.roll)。属实——确实"仅 s_agg 脚本"做了披露与 shift 变体。
指控引用的代码全部存在且被正确解读,不存在误读。

数据核对(duckdb + audit_c6_panel.parquet,脚本实际读取的面板):
per-quarter stamped shock 序列 n=82,lag-1 自相关 = 0.2707 ≈ 指控的 0.271;白噪声 SE=1/√82=0.1104 ≈ 0.110;0.2707/0.1104 = 2.45 个标准误 ≈ 指控的"约 2.5 个标准误"。全部逐位吻合,数字无夸大。

failure_scenario 的边际统计量也属实(读 output CSV):
- audit_ri_3pairwise.csv:LP cum4 RI p = 0.0730(指控写 0.073)。
- russia_ri_results.csv:Russia headline RI p = 0.1023(指控写 ≈0.10-0.11)。
这些确为近边际值,自由置换 vs 保序置换的差异可能推动其跨越/远离 0.05,而 headline/cum1/cum4/Russia/shocklag 五个系列均无 block 或 circular-shift 变体。

因此该行为(对持久性 GPR 冲击做自由置换、隐含可交换/白噪声、且未给 headline 系列补保序变体)真实发生,方法学上 AR(1)≈0.27 的持久性下自由置换确为近似,指控成立。仅凭"影响不大"不构成证伪。

缓解性证据(支持维持 MINOR,不支持证伪也不支持进一步降级为可忽略):
(1) 真正的 headline(headline dw 3-pairwise)RI p = 0.508,远离边际,任何保序变体都不会改变其推断;近边际的只是一个 LP 时点(cum4)和正对照(Russia),指控本身已把结论限定在"边际结论",非误读。
(2) 唯一同时给出两种 p 的 s_agg 规格里,circular-shift p=0.195 vs free p=0.232,仅移动 0.037 且两者都清晰落在零假设区,说明该持久度下实际位移量小。但这属于降低影响的论据,而非"场景不发生"。

结论:refuted=false,严重度维持 MINOR(已是最低有意义档,无需调整)。
**Opus反驳者2** refuted=False severity_adjust=keep MINOR (no downgrade); Russia sub-claim overstated (its shock is near-white-noise, autocorr -0.004), but core 3-pairwise/shocklag/headline scripts do permute the 0.271-autocorr shock and cum4 fails the circular-shift check (0.075 -> 0.231)
  B23 (MINOR) holds and is NOT refuted. (1) Code is exactly as described: run_ri_3pairwise.py:93 / run_ri_shocklag.py / run_ri_russia.py use free rng.permutation with no block/shift variant; only run_ri_sagg.py discloses and adds circular-shift. (2) The load-bearing fact checks out on the current audit_c6_panel.parquet: stamped-shock lag-1 autocorr = 0.2707 (n=82, WN SE 0.110, t=2.45), matching the claimed 0.271. (3) The failure scenario is CONFIRMED, not exaggerated: I ran the missing circular-shift RI (77 shifts) with the script's own two-way-demean algebra for the near-marginal 3-pairwise cum4 - free-perm p=0.0746 but circular-shift p=0.2308. Respecting the shock's serial structure destroys the sub-0.10 reading, exactly the anti-conservative direction B23 predicts. Repo's own sagg_ri_check (autocorr 0.357) corroborates: free 0.232 vs shift 0.195. So the free-permutation p on cum4 is not robust. It does NOT threaten the primary headline: h=0 RI p=0.508 (null anyway); the affected spec is a secondary LP horizon, so MINOR is the correct tier. (4) One scope inaccuracy that does not refute: B23 lists run_ri_russia.py as permuting the same 0.271-autocorr shock, but the Russia panel's shock is a DIFFERENT series (max abs diff 12.78 vs audit shock) with lag-1 autocorr -0.0039 - effectively white noise, so exchangeability is fine for Russia and its free 0.102 vs shift 0.159 gap is coarse 81-shift-grid noise. Net: finding validated at MINOR; keep severity, with the Russia sub-claim flagged as an overstatement of scope.

## [B24] status=CONFIRMED
**Opus反驳者1** refuted=False severity_adjust=keep minor
  B24 confirmed on all points; cannot refute. (1) run_ri_russia.py:37-49 computes the RI via US-NONUS pairwise difference + within-quarter demean of cn, which collapses to the it+gt (R1) FE only — the ig=group(firm_str,hgroup) firm-constant is NOT absorbed, so this is not the it+gt+ig 3-pairwise (R2) spec. B9's independent replication already verified this collapse. (2) Line 74 literally prints "(Stata CRVE 3-pairwise reference: b3=-7.327e-06, p=0.085)". (3) russia_ri_results.csv gives RI b3=-7.414427e-06, ri_p=0.1023, which matches russia_headline_results.csv it_gt column (us_ru_shock b=-7.41e-06), NOT the 3pairwise column (b=-7.33e-06, p=0.0853). The genuine 3-pairwise RI lives in a separate script (run_ri_russia_3pairwise.py -> russia_ri_3pairwise.csv, b3=-7.3265e-06), proving the two specs yield distinct b3s and that line 74's reference is mislabeled (R2 CRVE attached to an R1 RI). Given B9 (R1's CRVE is degenerate, se/p shown as '.'), this it+gt RI is in fact the only valid inference for that spec, and the printed reference should point to R1 with a note that its CRVE is unavailable — exactly as the claim states. The code exists, is not misread, and the mismatch occurs in the shipped outputs. Severity already MINOR and appropriate; no downgrade warranted.
**Opus反驳者2** refuted=False severity_adjust=keep MINOR
  B24 is confirmed at both code and data level; not refuted, severity MINOR stands.

CODE (julia_descriptive/run_ri_russia.py): The continuous-shock RI (lines 35-69) is computed as a within-firm-quarter US-NONUS pairwise difference (d_dw = MAX(us=1 dw) - MAX(us=0 dw), line 29) plus a within-quarter demean of cn (cn_c, line 37), solved via a 2-regressor closed form (cn_c, cn_c*S). The pairwise difference collapses firm-quarter FE (fq/it); the within-quarter demean absorbs group-quarter FE (gq/gt). There is NO firm-group intercept (ig) in the differenced equation, so the RI corresponds exactly to the it+gt spec (R1), NOT it+gt+ig (R2/3-pairwise). The docstring (lines 4-5) says as much: 'US-NONUS pairwise difference ... collapses the it+gt FE exactly.' Line 74 nonetheless prints '(Stata CRVE 3-pairwise reference: b3=-7.327e-06, p=0.085)' — verbatim as claimed.

DATA: russia_ri_results.csv -> b3 = -7.414427e-06, ri_p = 0.1023. russia_headline_results.csv -> it_gt (R1) us_ru_shock b = -7.41e-06 (se/p degenerate, consistent with B9); 3pairwise (R2) b = -7.33e-06, p = 0.0853. The RI b3 (-7.4144e-06) matches R1's it+gt estimate to displayed precision and does NOT equal the printed 3-pairwise reference (-7.327e-06 = R2 rounded). So the printed reference is on the wrong spec column. Every numeric assertion in the claim reproduces exactly.

This is a genuine spec-mislabel in a print statement: the RI computes the it+gt (R1) estimate but cites the 3-pairwise (R2) CRVE as its comparator. Because R1's own CRVE is degenerate (B9), this RI is in fact the only valid inference for the it+gt spec, so the mislabel would attach it to the wrong column if quoted. Consequence is contained (only a console print, not the output CSV or paper; R1 vs R2 point estimates differ ~1% and both give the same marginal-fail-at-5% reading), which is precisely why MINOR is appropriate and no downgrade is warranted. Fix: change line 74 to reference R1 (it+gt) and note its CRVE is unavailable.

## [B6] status=CONTESTED (existing Fable vote: refuted)
**Opus反驳者1** refuted=False severity_adjust=Downgrade MAJOR -> nit/info: code fact (ASOF-then-filter != BETWEEN join; comment overclaims equivalence; 94 real nested-overlap divergences) is true, but every predicted harm is data-immune — EU universe bitwise identical (2,578,678 both; 0 lost), all 94 divergent company-quarters have NULL region, and edge numerator/denominator diverge on 0 probes. Fix only the misleading 02:215-219 comment + optional cheap guard assertion; no downstream parquet rebuild needed.
  B6 复现审计结论:代码事实成立,但 failure_scenario 预言的所有后果在真实 Revere 数据上均不发生。用独立路径(Python duckdb 1.5.2 精确复刻,而非信任先前 refuter)对 E:/Data/Data/Factset Revere/revere_company_wrds.csv(3,897,161 行)与 data_giorgio.csv(5,556,608 行)重跑,同时构造 ASOF-then-filter(02:220-237/295-336 复刻)与真正的 BETWEEN 区间 join 两条实现并逐 probe 对比。

代码确如指控所述(故非 refuted=true):(1) rev_co_dedup(02:190-211)只按 (company_id,start_d) 去重,不消除嵌套重叠区间;(2) rev_co_asof_q 与 rev_rel 用 ASOF 选全局最新 start_d、再 WHERE/CASE 过滤 end_d,逻辑上确实不等于 BETWEEN 区间 join;(3) 02:215-219 注释"equivalent to the original BETWEEN-style join"是无条件过度声明。数据里确有 94 对嵌套重叠导致的 (company,qend) 分歧,证明非等价性真实存在。

但 failure_scenario 的每一条危害都被数据免疫:

USE 面(EU universe): BETWEEN 保留 13,432,986 对 vs ASOF 13,432,892,分歧恰 94 对;这 94 对的 BETWEEN 指派 home_region 全部为 NULL(逐值 breakdown: <NULL>=94, 其余 0),且这些 (company,qend) 下没有任何一条覆盖行带 EU region(any_eu_cover=1 计数=0)。因此 eu_revere_universe_qend 要求 home_region IN EU 元组(02:487),两种实现选出的 EU universe 完全一致:BETWEEN=ASOF=2,578,678 company-quarters,ASOF 相对 BETWEEN 丢失 0、多出 0。kept-both 的 region drift=0。指控预言的"公司退出 EU universe、其持仓在 05/06 失去 Revere 匹配"发生 0 次。

EDGE 面(分子 eu_china_edge / 分母 eu_any_edge): 在 distinct (endpoint, rel_start) probe 粒度上,src 端 702,126 probe、tgt 端 3,826,506 probe,两实现的指派 region 分歧 n_divergent=0(比先前 refuter 报的 6 更干净),asofNull_betwEU=0、asofNull_betwCN=0。即没有任何一条边在 BETWEEN 下会拿到 EU 或 CN region 却在 ASOF 下被置 NULL——numerator 与 denominator 逐位相同,china_share 分子分母不损失任何边,更无"与数据商 restatement 相关的非随机削减"。

故仅存的真实缺陷是 02:215-219 那句无条件等价注释误导。可复现证据脚本: scratchpad/b6_verify.py(USE 面)与 scratchpad/b6_edge.py(EDGE 面)。按任务定义(代码如指控但后果免疫→refuted=false+降级),应把 B6 从 MAJOR 降到 nit/info:把注释改成数据依赖的表述,可选加一句廉价断言(BETWEEN 覆盖但 ASOF 丢弃且 home_region 非 NULL 的 probe 数==0)以防未来数据刷新回归;无需因 B6 重建任何下游 parquet。

## [B10] status=CONFIRMED (existing Fable vote: stands)
**Opus反驳者1** refuted=False severity_adjust=维持 major
  B10 STANDS. refuted=false, severity maintained at MAJOR. I approached from a skeptic's angle and built an independent fixed-exposure counterfactual; both the code facts and the failure_scenario survive.

CODE FACTS (accusation accurate). build_russia_lp_panel.py:36 selects g.ru_lag (= russia_share_lag1q at the HORIZON quarter g); the base CTE (lines 31-33) fixes only w_base at 2021-12-31, NOT the exposure. run_russia_lp_test.py:5-6 docstring claims the regressor is "ru_lag, as of 2021Q4" and takes any_value(ru_lag) grouped by (firm, report_date), i.e. per horizon. Since russia_share_lag1q lags one quarter, only h=0 (2022Q1 -> russia_share@2021Q4) is pre-invasion/clean; h>=1 uses post-invasion exposure. Doc contradicts implementation — confirmed by reading both files.

REGRESSOR IS POST-TREATMENT AND DECAYS. In merged_us_ru_zero_filled.parquet, firm-holders with positive russia_share_lag1q fall 684->586 (grid) over 2022Q1->2023Q4; 230/684 = 34% of positive firm-holders recoded to exactly 0 by h=7 (742 changed). In the estimation sample n_pos falls 342->293. Divesting firms (highest exposure) migrate into the zero-exposure "control", exactly as the failure_scenario predicts.

FAILURE_SCENARIO MATERIALIZES (independent test). I rebuilt the LP panel with the exposure FIXED at its 2021Q4 value (what the doc claims) vs the repo's time-varying value, holding base weight and estimator identical:
- TIMEVARYING (repo) beta x1e-4: -1.51,-1.67,-1.58,-2.01,-1.13,-0.66,-1.09,-1.18 -> the h=3 (-2.01e-4) to h=7 (-1.18e-4) decay flagged in the claim.
- FIXED_2021Q4 beta x1e-4: -1.44,-1.53,-1.35,-1.74,-1.24,-1.15,-1.39,-1.55, n_pos constant at 335 -> STABLE, NO decay to h=7 (ends -1.55, not -1.18).
So the repo's h>=1 beta decay is substantially an artifact of the exposed cohort shrinking (treatment->control migration), not a genuine impulse-response attenuation. Cross-section composition differs by horizon; cannot be read as a same-cohort LP path.

h=1 SIGNIFICANCE IS CONTAMINATED. My vectorized permutation reproduces the repo perm_p (h=0 0.044 vs repo 0.0417; h=1 0.049 vs repo 0.0461). Under the FIXED (correct) spec: h=0 perm_p=0.0388 (still <0.05), but h=1 perm_p=0.0503 (falls just OUTSIDE 0.05). So the h=1 p<0.05 evidence in the positive control is a spec artifact of the post-invasion regressor; only h=0 is clean and survives.

MITIGATIONS (do not warrant downgrade). h=0 stays clean-negative and significant under both specs, so the positive control is not entirely dead. But the h>=1 LP path interpretation and the h=1 p<0.05 claim are unusable, matching the accusation precisely. Code is exactly as alleged and the predicted consequence is demonstrated in data -> cannot refute; MAJOR maintained.

Reproducible evidence: julia_descriptive/build_russia_lp_panel.py:31-40, julia_descriptive/run_russia_lp_test.py:5-6,22-28; output/russia_lp_test_results.csv; and the fixed-vs-timevarying rerun on output/merged_us_ru_zero_filled.parquet above.

## [B13] status=CONFIRMED (existing Fable vote: stands)
**Opus反驳者1** refuted=False severity_adjust=major → minor
  Code accurately described (refuted=false), and the underlying econometric critique of the WCB is even essentially valid, but the failure_scenario's load-bearing consequence is immune, so MAJOR downgrades to minor.

CODE FACTS CONFIRMED: run_audit_f1b_robust.do:33 runs `boottest us_cn_shock, weighttype(webb) reps(9999) bootcluster(rd_m)`; header lines 2-7 do frame this WCB as the remedy for "Overlapping cumulative windows + only 82 quarter-clusters." build_audit_panel_f1f2f7.py:72-76 confirms cum_h = w_{t+h} − w_prev are overlapping windows. The measurement critique holds structurally: two-way cluster(firm_n rd_m) covers within-firm and within-quarter correlation, but overlapping outcomes induce a cross-firm × adjacent-quarter correlation via the shared group-time normalization at shared calendar quarters, which a bootcluster(rd_m) WCB (resampling the 82 stamping-quarters as independent) does not repair. So the accusation's code reading and calculus are true.

WHY THE PREDICTED HARM DOES NOT OCCUR (independent, path-based verification, not just reviewer1's doc citations):
(1) No WCB output is persisted or used — output/ has only audit_f1_lp_irf.dta, no boottest/WCB log; a repo-wide grep finds ZERO citations of any WCB p-value or 'robust under bootstrap' phrasing. The boottest call is dead output.
(2) Essay2_methodology_full.md:601 reclassifies the file as 'F1b CRVE-vs-alternate-FE SENSITIVITY (see also RI)'; line 77 states the 'previously-planned wild cluster bootstrap FAILS ... Randomization inference ... is now the arbiter.' The only number used from this file is the anti-significance FE flip (CRVE 0.013→0.111). So the file is NOT treated as the formal remedy the accusation claims.
(3) The actual arbiter (RI) returns null, read directly from output/audit_ri_3pairwise.csv: LP cum1 β₃=+2.37e-06 RI p=0.234; LP cum4 β₃=+6.62e-06 RI p=0.073; headline +1.80e-06 p=0.508. Team conclusion is null, never 'LP holds under WCB.'
(4) DIRECTION defeats the premise: every LP point estimate is POSITIVE, opposite the β₃<0 disengagement hypothesis. Even a spuriously anti-conservative LP significance would reinforce the null, not support any team claim.
(5) The exact concern is already flagged: line 75 notes RI 'is approximate under residual serial correlation — a block-permutation / circular-shift variant ... deferred (R2-N1),' which is precisely B13's overlapping-window serial-correlation worry. So it is documented, not ignored.

Residual = a stale header comment on a superseded exploratory sensitivity script (documentation nit), not an active-path inference bug. Fix = delete/re-word the header or remove the file. Converges with reviewer1: refuted=false, major→minor.
